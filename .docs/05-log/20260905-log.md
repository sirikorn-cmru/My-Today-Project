# Log 20260905

- **สร้างสเปกใหม่ Sprint 14: Email/Password Login (Firebase Authentication):** สร้างไฟล์ `.docs/01-requirements/01-spec/20260905-016-my-today-sprint14-email-password-login.md` (running number 016) เป็น Sprint ที่ 3 ของ Version 3 ต่อจาก Sprint 12 (Cloud Sync) และ Sprint 13 (Smart Capture from Image) — เปิดผ่าน `requirement-intake` ตามช่องทางเดียวกัน ไม่ใช่การฝ่าฝืน Freeze ของ Version 2 เพิ่มวิธีล็อกอินด้วยอีเมล/รหัสผ่านเป็นวิธีที่สอง คู่กับ Google Sign-In เดิมของ Sprint 12 (ไม่แทนที่) — อ้างอิงชัดเจนว่าสเปกนี้คือการรับช่วงสิ่งที่ Sprint 12's Business Rule ข้อ 5 ("ไม่ทำ Email/Password ในรอบนี้") เลื่อนไว้ก่อน ไม่ใช่การขัดแย้งหรือแก้ไขสเปก Sprint 12 เดิม ครอบคลุม Business Rules สำคัญ: ไม่ต้อง Email Verification, บังคับมี "ลืมรหัสผ่าน" (`sendPasswordResetEmail`), แปล Firebase error code เป็นไทย, ไม่ merge บัญชี Google กับ Email/Password, ไม่กระทบ `firestore.rules`/`useCloudSync` เดิม, และ manual precondition ที่ต้องเปิด provider ใน Firebase Console เอง
- **อัปเดต `backlog.md`:** เพิ่ม entry Sprint 14 สถานะ "ยังไม่เริ่ม" ลิงก์กลับไปยัง [[../01-requirements/01-spec/20260905-016-my-today-sprint14-email-password-login|016]]
- **Implement Sprint 14 โค้ดเสร็จ + ยืนยันใน browser:** `src/hooks/useAuth.ts` เพิ่ม `signUpWithEmail`/`signInWithEmail`/`resetPassword` (ใช้ Firebase SDK เดิม ไม่เพิ่ม dependency) พร้อม `AUTH_ERROR_MESSAGES`/`translateAuthError` จุดเดียวที่ทุกฟังก์ชัน auth ใช้ร่วมกัน (รวม Google Sign-In เดิมด้วย); `ProfilePage` เพิ่มฟอร์ม email/password พร้อมสลับโหมด "เข้าสู่ระบบ"/"สมัครสมาชิก" + ปุ่ม "ลืมรหัสผ่าน" วางคู่กับปุ่ม Google เดิม; `App.tsx` ส่ง `signUpWithEmail`/`signInWithEmail`/`resetPassword` ลงไปเป็น prop ใหม่ — ยืนยันแล้วผ่าน Vite dev server จริงกับ Firebase project จริง (ไม่ใช่ mock): client-side validation disable ปุ่ม submit ถูกต้องเมื่ออีเมล/รหัสผ่านไม่ผ่านเกณฑ์, submit ยิง request จริงเจอ `auth/operation-not-allowed` จริง (เพราะยังไม่เปิด provider) แปลเป็นไทยถูกต้อง (เพิ่ม error code นี้เข้า mapping เพิ่มเติมจากสเปกเดิมเพราะเป็น error จริงที่เจอ), "ลืมรหัสผ่าน" เรียก `sendPasswordResetEmail` จริงสำเร็จและแสดงข้อความยืนยัน, ปุ่ม Google Sign-In เดิมไม่เปลี่ยนแปลง (non-regression); `npx tsc --noEmit` และ `npm run build` ผ่านสะอาดทั้งคู่ — ยังไม่ได้ยืนยันการสมัคร/เข้าสู่ระบบให้สำเร็จจริงเพราะยังไม่ได้เปิด provider "Email/Password" ใน Firebase Console (manual step ของ user) — อัปเดตสถานะใน `backlog.md`/`CLAUDE.md` เป็น "โค้ดเสร็จแล้ว แต่ยังไม่ผ่าน Gate 14" ยังไม่ commit/push รอ user ยืนยันตามธรรมเนียมเดิม
- **User เปิด Email/Password provider ใน Firebase Console แล้ว ทดสอบจริงสำเร็จ — Gate 14 ผ่าน:** ทดสอบสมัครสมาชิกจริงด้วย `sprint14-verify@my-today-test.com` รอบแรกยังเจอ `auth/operation-not-allowed` (provider ยังไม่ enabled จริง แม้ user จะบอกว่า save แล้ว) ให้ user ไปเช็ค Console ซ้ำ พบว่าต้องกด Enabled ให้ครบขั้นตอนจริงๆ — หลังจากนั้นทดสอบใหม่: (1) สมัครสมาชิกสำเร็จ ขึ้น "ลงชื่อเข้าใช้แล้ว" ทันทีไม่ต้องยืนยันอีเมล, (2) ออกจากระบบแล้วเข้าสู่ระบบซ้ำด้วยอีเมล/รหัสผ่านเดิมสำเร็จ (ระหว่างทดสอบเจอ `auth/invalid-credential` เพราะกรอกข้อมูลทดสอบผิดเอง ไม่ใช่บั๊ก แก้แล้วผ่าน), (3) ปุ่ม Google Sign-In เดิมไม่เปลี่ยนแปลง — สรุปว่า Gate 14 ผ่านครบสำหรับกลไก auth หลัก อัปเดต `backlog.md`/`CLAUDE.md` เปลี่ยนสถานะเป็น "เสร็จแล้ว และยืนยันจริงกับ Firebase project จริงแล้ว" พร้อมหมายเหตุว่าการทดสอบ cross-device Cloud Sync แยกสำหรับบัญชี Email/Password (แบบ Incognito ของ Sprint 12) ยังไม่ได้ทำซ้ำในรอบนี้ — commit และ push ตามคำสั่ง user
- **⚠️ พบและแก้ไข incident จริง — Firestore Security Rules ที่ deploy อยู่จริงเป็น default deny-all ไม่ใช่กฎจากโค้ด:** ระหว่างทดสอบ cross-device sync ของ Sprint 14 เปิด Cloud Sync ด้วยบัญชี Email/Password แล้วเจอ error จริง "Missing or insufficient permissions" ให้ user เปิด Firebase Console → Firestore Database → Rules ดู พบว่ากฎที่ deploy จริงคือ `allow read, write: if false;` (ค่า default ที่บล็อกทุกอย่าง) ไม่ใช่กฎ `request.auth.uid == uid` จากไฟล์ `firestore.rules` ในโค้ดเลย — ให้ user ลองสลับไปใช้บัญชี Google Sign-In เดิมก็เจอปัญหาเดียวกัน ยืนยันว่ากระทบทุก provider เท่ากัน ไม่ใช่บั๊กเฉพาะ Email/Password — ให้เนื้อหากฎที่ถูกต้องจากไฟล์ในโค้ดไปวางแทนใน Console แล้วให้ user กด Publish
- **ยืนยัน fix สำเร็จ + cross-device sync ผ่านครบทั้งสอง provider:** หลัง publish กฎใหม่ ทดสอบซ้ำ: (1) Google Sign-In (บัญชีจริงของเจ้าของโปรเจกต์) เปิด Cloud Sync ขึ้น "sync สำเร็จ" ทันที, (2) สลับมาบัญชี Email/Password ทดสอบ สร้าง Task ทดสอบ "SPRINT14-SYNC-TEST" ขณะเปิด sync แล้วรอ push เสร็จ จากนั้นใช้ `javascript_tool` ล้าง localStorage/sessionStorage/IndexedDB ทั้งหมด (รวม `firebaseLocalStorageDb`) จำลอง "อุปกรณ์ใหม่" ที่ไม่มีข้อมูล/session เดิมเลย — reload แล้ว sign in ด้วยบัญชีเดิมซ้ำ เปิด Cloud Sync อีกครั้ง **Task "SPRINT14-SYNC-TEST" ปรากฏขึ้นเองทันทีในหน้า Tasks โดยไม่ต้องสร้างใหม่** เทียบเท่าหลักฐานการทดสอบ Incognito ของ Sprint 12 — ยืนยัน cross-device sync ของ Sprint 14 ผ่านครบทุก Acceptance Criteria แล้ว
- **อัปเดตเอกสาร:** แก้ `backlog.md`'s Sprint 12 entry เพิ่มหัวข้อ incident นี้พร้อมข้อสังเกตว่าการยืนยัน "multi-device sync สำเร็จ" ของ 20260901 ต้องเกิดตอนกฎยัง deploy ถูกต้อง แล้วมีบางอย่างทำให้ถูกรีเซ็ตกลับเป็น default ภายหลัง (ไม่ทราบสาเหตุแน่ชัด), เพิ่มบทเรียนว่าควรรัน `npm run test:rules` ให้ได้จริงเพื่อจับปัญหานี้อัตโนมัติในอนาคต; แก้ Sprint 14 entry เปลี่ยนหมายเหตุ "ยังไม่ได้ทดสอบ cross-device sync" เป็นยืนยันสำเร็จแล้วพร้อมรายละเอียดวิธีทดสอบ; อัปเดต CLAUDE.md's Project state เพิ่มหมายเหตุ incident เดียวกัน
- **แก้ `package.json`'s `test:rules` script:** เปลี่ยนจากเรียก `firebase-tools` ตรงๆ (ไม่มี binary ติดตั้งจริงในโปรเจกต์/global เลย พังทันทีด้วย "not recognized") เป็น `npx --yes firebase-tools ...` แทน เพื่อให้ดึงมารันได้เองโดยไม่ต้อง install ล่วงหน้า — ทดสอบยืนยันว่า error เปลี่ยนจาก "not recognized" เป็น error เรื่อง Java version ตรงๆ (แปลว่าจุดนี้แก้ถูกจุดแล้ว)
- **รัน `npm run test:rules` สำเร็จจริงเป็นครั้งแรก — ALL CHECKS PASSED:** เครื่อง sandbox นี้มีแค่ Java 8 มาตลอด ลอง `choco install temurin21` ก่อนแต่ติด error สิทธิ์ admin ของ Chocolatey (ไม่ได้รันจาก elevated shell) จึงเปลี่ยนวิธีเป็นดาวน์โหลด Eclipse Temurin JDK 21 แบบ portable (.zip, ~195MB จาก Adoptium/GitHub) มาแตกไว้ใน scratchpad แล้วรันคำสั่งด้วย `JAVA_HOME`/`PATH` ชี้ไปที่ JDK ตัวนั้นชั่วคราวแทนการติดตั้งระบบ — รัน `npm run test:rules` ผ่าน Firestore Emulator จริงสำเร็จ **ALL CHECKS PASSED** ครบ 7 เคส (Alice เขียน/อ่านงานตัวเองได้, Bob อ่าน/เขียนงานของ Alice ไม่ได้, unauthenticated อ่าน/เขียนไม่ได้เลย, Bob เขียน/อ่านงานของตัวเองได้) ยืนยัน logic ของ `firestore.rules` ถูกต้องตามที่เคยตรวจด้วยมือไว้ทุกประการ — **ปิด Gate 12's Security Rules acceptance criterion ได้สมบูรณ์ ไม่มีรายการค้างจาก Sprint 12 อีกต่อไป**
- **อัปเดตเอกสารรอบสุดท้าย:** เปลี่ยนสถานะ Sprint 12 ใน `backlog.md` จาก "กำลังดำเนินการ" เป็น "เสร็จแล้วสมบูรณ์" (checkbox `[x]`), เพิ่มรายละเอียดการรัน test:rules สำเร็จ, อัปเดต "สรุป" ท้าย entry ให้สะท้อนว่าไม่มีรายการค้างแล้ว; อัปเดต CLAUDE.md's Commands section (`test:rules` ตอนนี้ผ่านจริงแล้ว) และ Project state paragraph (Sprint 12 code-complete → complete, Gate 12 passed in full)
