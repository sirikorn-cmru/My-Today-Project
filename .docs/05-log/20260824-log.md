# Log 20260824

- ตรวจสอบ backlog.md ผ่าน backlog-sync-check: พบว่ารายการ Sprint 9 (Now/Next/Later Timeline + Smart Priority + Life Progress, spec 010) ยังระบุสถานะ "ยังไม่เริ่ม" ทั้งที่ตรวจโค้ดจริงใน working tree พบหลักฐาน build ครบตาม scope — src/lib/timelineUtils.ts (ใหม่, `smartPriorityTier`/`sortTasksBySmartPriority` ตาม Business Rule 2, `getTimelineEntries` ตาม Business Rule 1, `getLifeProgress` ตาม Business Rule 3 ไม่มีคำว่า "Score"), src/components/LifeProgress.tsx + src/components/TimelineSection.tsx (ใหม่), src/pages/TimelinePage.tsx (ใหม่, route `/timeline`), src/components/NavBar.tsx (เพิ่มแท็บ Timeline), src/App.tsx (wiring route), src/pages/DashboardPage.tsx (เรียง Today's Tasks ด้วย Smart Priority + แสดง Life Progress) — แต่ยังไม่มี commit "Implement Sprint 9" ขึ้น git log ณ วันที่ตรวจสอบ (การเปลี่ยนแปลงยังอยู่ใน working tree เท่านั้น) — แก้ไข backlog.md ให้สถานะ Sprint 9 เป็น "เสร็จแล้ว" พร้อมหมายเหตุอ้างอิงหลักฐานไฟล์และระบุชัดว่ายังไม่มี commit แทนการอ้าง commit hash
- อัปเดต feature-list.md และ user-journey-*.md (feature-journey-sync) ให้สอดคล้องกับ backlog.md ที่เพิ่งยืนยันสถานะ Sprint 9 ว่า "เสร็จแล้ว": feature-list.md เปลี่ยนสถานะ Sprint 9 (ตารางสรุป + subsection) จาก "ยังไม่เริ่ม" เป็น "เสร็จแล้ว" โดยไม่เปลี่ยน MoSCoW (ยังคง Should ตามเหตุผลเดิม); user-journey-student.md และ user-journey-general-person.md ย้ายขั้นตอน Timeline (FR-16) และ Life Progress (FR-17) จากสไตล์ `planned` (เส้นประ, "แผนในอนาคต") เป็น `done` (เส้นทึบ) ทั้งในไดอะแกรมและรายการขั้นตอน พร้อมอัปเดตหมายเหตุสถานะเป็นวันที่ 20260824 — คงขั้นตอน Reminder Lead Time (FR-19, Sprint 10) ไว้เป็น `planned` เช่นเดิมเนื่องจาก Sprint 10 ยังไม่เริ่ม
- อัปเดต `.docs/02-design/02-technical/architecture.md` (architecture-doc-sync) ให้สะท้อน Sprint 9 เสร็จแล้ว: ขยายคำอธิบาย Application/Domain Logic Layer ในหัวข้อ 2 ให้ระบุว่า derive มุมมอง Now/Next/Later และ completion-progress aggregate ด้วย (ไม่แยกเป็น container ใหม่ ตามการตัดสินใจว่าไม่มี read-state ใหม่ต้อง persist); เพิ่มประโยคในหัวข้อ 3 (Core Domain Concepts) ว่าฟิลด์เดิมของ Task/Event เป็นฐานของมุมมอง derived นี้โดยไม่มี entity ใหม่; ปรับสถานะแถว FR-16/FR-17 ในตาราง Data Flow ทั้งสอง persona (หัวข้อ 4) จาก "แผนในอนาคต" เป็น "เสร็จแล้ว"; ตัด known gap ของ Sprint 9 ออกจากหัวข้อ 6 เหลือเฉพาะ Sprint 10-11; เพิ่มบรรทัด Change Log ใหม่
- อัปเดต `.docs/02-design/02-technical/database-schema.md` และ `api-spec.md` (data-api-doc-sync) ให้สะท้อน Sprint 9 เสร็จแล้ว โดยยืนยันว่า Sprint 9 ไม่เพิ่มฟิลด์/entity ใหม่เลย (เป็น derived logic ล้วนๆ): database-schema.md ลบ bullet Timeline/Life Progress ออกจากหัวข้อ 4 Known Gaps (ไม่มี replacement เพราะไม่มีการเปลี่ยน schema); api-spec.md ย้าย "Get Timeline (Now/Next/Later)", "Smart Priority Sort", "Get Life Progress" จากหัวข้อ 4 Known Gaps เข้าหัวข้อ 3 Derived/Read-only Operations พร้อมรายละเอียด Input/Output/Business Rule เต็ม — เพิ่มบรรทัด Change Log ในทั้งสองไฟล์ ไม่แตะ ER diagram/รายละเอียดตาราง/operation contract ตารางเดิม/known gap ของ Sprint 10-11
- อัปเดต `.docs/02-design/02-technical/detailed-design.md` (detailed-design-doc-sync) ให้สะท้อน Sprint 9 เสร็จแล้ว: เพิ่ม sequence message จริงสำหรับ Timeline (Now/Next/Later + Smart Priority Sort) และ Life Progress ในไดอะแกรม persona ทั้งสอง (§1.1 ขั้นตอนที่ 7/11, §1.2 ขั้นตอนที่ 5/8) แทนที่โน้ต "ยังไม่รวมในไดอะแกรมนี้" เดิม พร้อมอัปเดต mapping list ให้ระบุ FR-16/FR-17 และสถานะ "เสร็จแล้ว" (คงเหลือเฉพาะขั้นตอนที่ 9 ของนักศึกษา — Custom Reminder Lead Time, Sprint 10 — ที่ยังข้ามไดอะแกรม); เพิ่มโน้ตใน §3 ว่าสอง operation นี้เป็น read-only derivation ล้วนๆ เหมือน Build Notifications; ลบ known-gap bullet 2 รายการของ Sprint 9 ออกจาก §4 ให้ตรงกับ architecture.md/api-spec.md; เพิ่มหมายเหตุการ implement ใหม่ใน §5 อ้างอิงฟังก์ชันจริง (`getTimelineEntries`, `smartPriorityTier`, `sortTasksBySmartPriority`, `getLifeProgress` ใน `src/lib/timelineUtils.ts`) — ตรวจพบว่า §4 เดิมมี bullet Sprint 9 อยู่จริง (ต่างจากที่คาดไว้ตอนแรกว่าไม่มี) จึงต้องลบออกเพื่อความสอดคล้อง; ไม่แตะไดอะแกรม/mapping ของหัวข้อ 2 (cross-cutting operations) และไม่พบ conflict กับ architecture.md/api-spec.md/database-schema.md
- ตรวจสอบ backlog.md ซ้ำ (backlog-sync-check) หลังจาก commit `e453ba7` (Implement Sprint 9, แก้เฉพาะ `src/`) และ `eebf267` (Sync docs, แก้เฉพาะ `.docs/`) ถูก push ขึ้นแล้ว: พบว่ารายการ Sprint 9 ยังมีหมายเหตุเดิมที่ค้างอยู่ว่า "ยังไม่มี commit ... ยังอยู่ใน working tree เท่านั้น" ซึ่งล้าสมัยแล้ว — แก้ไขหมายเหตุให้อ้างอิง commit hash จริง (`e453ba7`, `eebf267`) แทน; ยืนยันว่าสเปกทั้ง 13 ไฟล์ (001-013) มี backlog entry ที่ลิงก์ไม่เสียครบทุกไฟล์ และ Sprint 10-11 ยังคงสถานะ "ยังไม่เริ่ม" ถูกต้องตรงกับ `src/` และ git log (ไม่พบ commit "Implement Sprint 10/11" และไม่พบโค้ดของฟีเจอร์ Sprint 10-11 เช่น Note/Link linking บน Task/Event, custom reminder lead time, IndexedDB quota-warning)
- ตรวจสอบ backlog.md อีกครั้ง (backlog-sync-check) หลัง Sprint 10 (Task–Event–File Linking, spec 011) ถูก implement ใน working tree: พบว่ารายการ Sprint 10 ยังระบุสถานะ "ยังไม่เริ่ม" ทั้งที่ตรวจโค้ดจริงพบหลักฐาน build ครบตาม scope — `src/types.ts` มี `Task.linkedNoteIds`/`linkedLinkIds`/`reminderLeadTime`, `CalendarEvent` เช่นเดียวกัน, `FileRecord.linkedEventIds` ใหม่ (Business Rule 4); `src/components/TaskDetailModal.tsx`/`EventDetailModal.tsx` (ใหม่ทั้งคู่) จัดหน้า What/When/Information ครบตาม Business Rule 3 พร้อม reminder lead time selector (`src/lib/taskUtils.ts` มี `reminderLeadTimeOptions`/`formatLeadTime`); `src/lib/notificationUtils.ts` override threshold default ของ Sprint 5 ด้วย `reminderLeadTime` ตาม Business Rule 2; `src/hooks/useFiles.ts` เพิ่ม `linkFileToEvent`/`unlinkFileFromEvent`; normalize-on-read ครบใน `useTasks.ts`/`useEvents.ts`/`useFiles.ts`; `TaskCard.tsx`/`DayAgenda.tsx` มีปุ่ม "รายละเอียด" wiring ผ่าน `App.tsx` ครบ; `TaskFormModal.tsx` คง field ใหม่ไว้ไม่ล้างตอนแก้ไขผ่านฟอร์มปกติ; seed data ครบทั้ง 3 ไฟล์ — แต่ยังไม่มี commit "Implement Sprint 10" ขึ้น git log ณ วันที่ตรวจสอบ (การเปลี่ยนแปลงยังอยู่ใน working tree เท่านั้น) — แก้ไข backlog.md ให้สถานะ Sprint 10 เป็น "เสร็จแล้ว" พร้อมหมายเหตุอ้างอิงหลักฐานไฟล์และระบุชัดว่ายังไม่มี commit แทนการอ้าง commit hash
- อัปเดต feature-list.md และ user-journey-*.md (feature-journey-sync) ให้สอดคล้องกับ backlog.md ที่เพิ่งยืนยันสถานะ Sprint 10 ว่า "เสร็จแล้ว": feature-list.md เปลี่ยนสถานะ Sprint 10 (ตารางสรุป + subsection) จาก "ยังไม่เริ่ม" เป็น "เสร็จแล้ว" โดยไม่เปลี่ยน MoSCoW (ยังคง Must ตามเหตุผลเดิม) และไม่แตะ Sprint อื่น/FR-ID column; user-journey-student.md ย้ายขั้นตอนที่ 9 (Reminder Lead Time เอง, FR-19) จากสไตล์ `planned` (เส้นประ, "แผนในอนาคต") เป็น `done` (เส้นทึบ) ทั้งในไดอะแกรมและรายการขั้นตอน พร้อมอัปเดตหมายเหตุสถานะปิดท้ายเป็นวันที่ 20260824 ระบุว่า Sprint 1-10 เสร็จหมดแล้ว เหลือ Sprint 11 เป็น Sprint เดียวที่ "ยังไม่เริ่ม"; user-journey-general-person.md ตรวจสอบแล้วไม่มีขั้นตอนใดอ้างอิง Sprint 10/FR-18 โดยตรง จึงอัปเดตเฉพาะหมายเหตุสถานะปิดท้ายให้ตรงกับวันที่/สถานะ backlog.md ปัจจุบันเช่นกัน โดยไม่แก้ไดอะแกรมหรือ mapping list
- อัปเดต `.docs/02-design/02-technical/architecture.md` (architecture-doc-sync) ให้สะท้อน Sprint 10 (Task-Event-File Linking — What/When/Information) เสร็จแล้ว: เพิ่มความสัมพันธ์ใหม่ในหัวข้อ 3 (Core Domain Concepts) — Task↔Note, Task↔Link, Event↔Note, Event↔Link (เก็บฝั่ง Task/Event ตั้งใจสวนทางกับทิศทาง File↔Task เดิม) และ File↔Event (เก็บฝั่ง File แบบเดียวกับ File↔Task) พร้อมประโยคอธิบาย Reminder lead time แบบกำหนดเองต่อรายการที่เชื่อมกับ Reminder/Notification Derivation container ในหัวข้อ 2; ปรับสถานะแถว FR-19 ในตาราง Data Flow ของ journey นักศึกษา (หัวข้อ 4) จาก "แผนในอนาคต" เป็น "เสร็จแล้ว" (journey บุคคลทั่วไปไม่มีแถวนี้ คงไว้ตามเดิม); ตัด known gap bullet ของ Sprint 10 ออกจากหัวข้อ 6 เหลือเฉพาะ 2 รายการที่ผูกกับ Sprint 11 (Accessibility Baseline, IndexedDB Quota-Warning) พร้อมแก้ประโยคเปิดหัวข้อให้ระบุว่าเหลือเฉพาะ Sprint 11 ที่ยังไม่ build; เพิ่มบรรทัด Change Log ใหม่ — ไม่มี container ใหม่เกิดขึ้น (ตามการตัดสินใจล่วงหน้า) จึงไม่แตะหัวข้อ 1 (Context), ไดอะแกรม/หมายเหตุ implement ของหัวข้อ 2 (Container View), หรือหัวข้อ 5 (Cross-cutting Principles)
- อัปเดต `.docs/02-design/02-technical/database-schema.md` และ `api-spec.md` (data-api-doc-sync) ให้สะท้อน Sprint 10 (Task-Event-File Linking — What/When/Information) เสร็จแล้ว ตรวจสอบตรงกับ `src/types.ts` แล้ว: database-schema.md เพิ่มความสัมพันธ์ `TASK}o--o{NOTE`/`TASK}o--o{LINK`/`EVENT}o--o{NOTE`/`EVENT}o--o{LINK` (ถือฝั่ง Task/Event) และ `EVENT}o--o{FILE` (ถือฝั่ง File เหมือนเดิม) ในหัวข้อ 1 ER Diagram พร้อมฟิลด์ `linkedNoteIds`/`linkedLinkIds`/`reminderLeadTime` บน TASK/EVENT และ `linkedEventIds` บน FILE, เพิ่มแถวฟิลด์และกฎทางธุรกิจใหม่ในหัวข้อ 2.2 (Task)/2.3 (Event)/2.4 (File) เน้นย้ำทิศทางการถือรายการที่สวนทางกันโดยเจตนา, ลบ bullet Sprint 10 ทั้งสามรายการออกจากหัวข้อ 4 Known Gaps (เหลือเฉพาะ IndexedDB Quota-Warning ของ Sprint 11); api-spec.md เพิ่ม operation คู่ Link/Unlink Note และ Link/Unlink Link ให้ Task (1.2) และ Event (1.3) ตามรูปแบบ dedicated operation เดียวกับ "Link File to Task", เพิ่ม "Set Custom Reminder Lead Time" ทั้งสอง entity พร้อมระบุพฤติกรรมยุบจากสอง tier (early-warning/imminent ของ Sprint 5) เหลือ tier เดียวเมื่อ override, เพิ่ม "Link/Unlink File to/from Event" และ "Get Related Files for Event" ในหัวข้อ 1.4, ย้าย "Get Task/Event Detail" จากหัวข้อ 4 Known Gaps เข้าหัวข้อ 3 Derived/Read-only Operations เป็นสอง operation แยก, ลบ bullet Sprint 10 ออกจากหัวข้อ 4 — เพิ่มบรรทัด Change Log ในทั้งสองไฟล์ ตรวจสอบสอดคล้องกับ architecture.md หัวข้อ 3 ที่อัปเดตแล้วก่อนหน้า ไม่พบ conflict ใดๆ
- อัปเดต `.docs/02-design/02-technical/detailed-design.md` (detailed-design-doc-sync) ให้สะท้อน Sprint 10 (Task-Event-File Linking — What/When/Information) เสร็จแล้ว: เพิ่ม sequence message จริงสำหรับขั้นตอนที่ 9 (Reminder lead time ที่ตั้งเอง) ในไดอะแกรม persona นักศึกษา (§1.1) แทนที่โน้ต "ยังไม่รวมในไดอะแกรมนี้" เดิม พร้อมอัปเดต mapping list ให้ระบุ FR-19 และสถานะ "เสร็จแล้ว" (journey บุคคลทั่วไปตรวจสอบแล้วไม่มีขั้นตอนอ้างอิง Sprint 10 จึงไม่แก้ §1.2); เพิ่ม §2.5 ใหม่ "Get Task Detail / Get Event Detail" เป็น sequence diagram cross-cutting ตัวที่ห้าในหัวข้อ 2 (แม้จะอยู่ใน api-spec.md หัวข้อ 3 ไม่ใช่หัวข้อ 2 ก็ตาม เพราะมีลักษณะ cross-cutting เดียวกับ List Inbox Items); เพิ่มโน้ตใน §3 สำหรับ Link/Unlink Note/Link ↔ Task/Event + Set Custom Reminder Lead Time (field update ธรรมดา ไม่มี relationship record แยกเหมือน File) และสำหรับ Get Task/Event Detail (read-only ล้วนๆ); ลบ known-gap bullet ทั้งสามรายการของ Sprint 10 ออกจาก §4 ให้ตรงกับ architecture.md/api-spec.md เหลือเฉพาะ Sprint 11 (IndexedDB Quota-Warning); เพิ่มหมายเหตุการ implement ใหม่ใน §5 อ้างอิงฟังก์ชัน/component จริงที่ตรวจสอบจากซอร์สโค้ดโดยตรง — `src/components/TaskDetailModal.tsx`/`EventDetailModal.tsx` สำหรับ Get Task/Event Detail, `taskLevel`/`eventLevel` ใน `src/lib/notificationUtils.ts` สำหรับ custom reminder lead time override, ยืนยันว่า Link/Unlink Note/Link ↔ Task/Event ใช้ `onUpdateTask`/`onUpdateEvent` ทั่วไป ไม่ใช่ dedicated hook method แยก — ไม่พบ conflict กับ architecture.md/api-spec.md/database-schema.md ที่อัปเดตแล้วก่อนหน้าในวันเดียวกัน; ใช้ tech-stack.md ที่มีอยู่แล้วเป็นแหล่งอ้างอิงเวอร์ชันเทคโนโลยี (React `^18.3.1` เป็นต้น) ไม่ต้องอ่าน package.json ตรงๆ
