# Log 20260828

- **Black Box Testing (Sprint 7-10, ทางการครั้งแรก):** รันทดสอบจริงในเบราว์เซอร์ (ไม่ใช่แค่เขียน test case สมมติ) บนข้อมูลที่ล้างใหม่ทั้ง `localStorage`/IndexedDB เพื่อให้ทดสอบซ้ำได้จาก seed data — ครอบคลุมทุก FR ของ Competition Track Sprint 7-10 ที่ [[../01-requirements/01-spec/20260806-012-my-today-sprint11-competition-demo-freeze|Sprint 11]] ระบุไว้เป็นงานค้าง: FR-01/FR-02 (Personal Profile + Life Area CRUD รวม cascade-safe delete), FR-13 (Quick Capture ครบ 5 ประเภท Task/Event/Note/Link/File), FR-14 (Universal Inbox + จัดเข้า Life Area), FR-15 (Note/Link organize flow), FR-16 (Timeline Now/Next/Later + Smart Priority — พิสูจน์กฎ "Overdue ขึ้นก่อนเสมอไม่ว่า Priority จะเป็นอะไร" ด้วย Task Priority ต่ำที่เลยกำหนดเทียบกับ Priority สูงที่ยังไม่ถึงกำหนด), FR-17 (Life Progress แยกตาม Life Area ไม่มีคำว่า "Score"), FR-18 (Task What/When/Information รวมไฟล์+บันทึก+reminder ในหน้าเดียว พร้อมยืนยัน non-regression ของ Task-File link เดิมจาก Sprint 4), FR-19 (custom reminder lead time override default ของ Sprint 5 จริง — พิสูจน์ด้วยการเทียบ Task 2 ตัวที่กำหนดส่งวันเดียวกันแต่ผลการแจ้งเตือนต่างกันเพราะตั้ง custom reminder ต่างกัน) รวม 19 Test Case **ผ่านทั้งหมด ไม่พบบั๊กจริงในโค้ด** ทุกผลยืนยันด้วยการอ่านค่าจริงจาก `localStorage`/IndexedDB ไม่ใช่แค่ดูหน้าจอ — บันทึกผลละเอียดที่ [[../03-testing/02-test-result/20260828-black-box-test-result|20260828-black-box-test-result.md]] (เอกสารแรกของโฟลเดอร์ `.docs/03-testing/02-test-result/` ที่เคยว่างมาตลอด) พบข้อสงสัย 2 จุดระหว่างทดสอบ ตรวจสอบโค้ดจริงแล้วยืนยันเป็นพฤติกรรมถูกต้องตามออกแบบทั้งคู่ ไม่ใช่บั๊ก: (1) Link Quick Capture ไม่บังคับกรอก URL ตั้งใจตาม Sprint 8 Business Rule ข้อ 1 (ทุก entity capture ผ่าน Quick Capture ต้องการแค่ชื่ออย่างเดียว) — คำอธิบายใน `api-spec.md` ที่ดูขัดกันหมายถึงฟอร์มแบบเต็มเท่านั้น ไม่ใช่ Quick Capture, (2) timing ของการอ่านค่า `disabled` หลัง set input ผ่านเครื่องมือทดสอบเป็นข้อจำกัดของ React 18 batching ไม่ใช่บั๊กแอป
- **backlog.md:** อัปเดต Sprint 11 entry แบบ targeted — ย้าย "Black Box Testing แบบเป็นทางการครบทุก FR ของ Sprint 7-10" จากรายการ "ยังไม่ได้ทำ" ไปเป็นรายการ "ทำเสร็จแล้วเพิ่มเติม (ตรวจสอบ 20260828)" พร้อมลิงก์ไปยังเอกสารผลทดสอบ, เพิ่มหมายเหตุ checkpoint 20260828 ที่หัวรายการ Sprint 11, และปรับหมายเหตุสำคัญท้ายรายการให้ระบุชัดว่า Black Box Testing เสร็จแล้วแต่ยังไม่ commit ณ เวลาที่เขียน — สถานะยังคง **"กำลังดำเนินการ"** เหมือนเดิม ไม่เปลี่ยนเป็น "เสร็จแล้ว" เพราะ Browser Compatibility Matrix (รันข้าม browser จริง), re-verify UX/UI มาตรฐาน Sprint 6, และ Gate 11 ยังไม่ได้ทำ — ยังไม่ commit/push รอ user ยืนยันตามธรรมเนียมเดิมของโปรเจกต์นี้
