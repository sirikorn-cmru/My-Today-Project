# Log 20260920

- **สร้างสเปกใหม่ Sprint 15: Smart Capture จากรูปภาพ สำหรับ Quick Capture ประเภท Task:** สร้างไฟล์ `.docs/01-requirements/01-spec/20260920-017-my-today-sprint15-smart-capture-task.md` (running number 017) เป็น Sprint ที่ 4 ของ Version 3 ต่อจาก Sprint 12 (Cloud Sync), Sprint 13 (Smart Capture from Image — Event เท่านั้น) และ Sprint 14 (Email/Password Login) — เปิดผ่าน `requirement-intake` ตามช่องทางเดียวกัน ไม่ใช่การฝ่าฝืน Freeze ของ Version 2 ขยายฟีเจอร์ Smart Capture from Image ของ Sprint 13 ให้ใช้กับ Quick Capture ประเภท "งาน (Task)" ได้ด้วย นอกเหนือจาก Event เดิม — อ้างอิงชัดเจนว่าสเปกนี้คือการรับช่วงสิ่งที่ [[../01-requirements/01-spec/20260830-015-my-today-sprint13-smart-capture-image|Sprint 13's Business Rule ข้อ 1]] กันขอบเขตไว้เฉพาะ Event เท่านั้น ไม่ใช่การขัดแย้งหรือแก้ไขสเปก Sprint 13 เดิม (pattern เดียวกับที่ Sprint 14 รับช่วง Email/Password ที่ Sprint 12 กันไว้) ครอบคลุม Business Rules สำคัญ: เพิ่มฟิลด์ใหม่ `location: string` ให้ `Task`/`TaskInput` แบบ pure addition ไม่ bump storage key (`my-today:tasks:v2` คงเดิม, ใช้ pattern เดียวกับ `inInbox`/`updatedAt`), รีใช้ `SmartCaptureModal`/`/api/smart-capture` เดิมของ Sprint 13 ทั้งหมดไม่สร้างซ้ำ (ปรับแค่ mapping ผลลัพธ์ `date`→`dueDate`, `startTime`→`dueTime`, `location`→`location`), ต้อง sign in ก่อนใช้เหมือนเดิม, AI เติมฟอร์มให้ตรวจสอบ/แก้ไขก่อนบันทึกเสมอไม่ auto-submit (สืบทอดจาก Sprint 13), และ non-regression เต็มรูปแบบต่อ Event/File/Note/Link
- **อัปเดต `backlog.md`:** เพิ่ม entry Sprint 15 สถานะ "ยังไม่เริ่ม" ลิงก์กลับไปยัง [[../01-requirements/01-spec/20260920-017-my-today-sprint15-smart-capture-task|017]]
- **Implement Sprint 15 โค้ดเสร็จ + ยืนยันใน browser:** `src/types.ts` เพิ่ม `Task.location: string` (pure addition), `src/hooks/useTasks.ts` normalize record เก่าเป็น `''`, `src/data/seedTasks.ts` อัปเดต seed ทั้ง 7 records ให้มีฟิลด์นี้; `src/components/TaskFormModal.tsx` เพิ่มฟิลด์ "สถานที่" ในฟอร์มปกติ + เพิ่ม `prefill?: Partial<TaskInput>` prop (pattern เดียวกับ `EventFormModal`); `src/components/SmartCaptureModal.tsx` แปลงเป็น generic component — export `SmartCaptureExtractedFields` type ใหม่ (`{title?, date?, startTime?, location?}`) แทนการผูกกับ `CalendarEventInput` ตรงๆ, เพิ่ม prop `kind?: 'task' | 'event'` สำหรับ label เท่านั้น (ไม่กระทบ API call), แก้ข้อความ "ต้อง sign in ด้วย Google" เป็น "ต้อง sign in" เฉยๆ เพราะ Sprint 14 มี Email/Password แล้ว; `src/components/QuickCaptureModal.tsx` เปลี่ยน `EventMode` เป็น `CaptureMode` ใช้ร่วมกัน เพิ่ม state `taskMode`/`taskPrefill` และ chooser "กรอกฟอร์มเอง"/"สแกนจากรูปภาพ" ให้ kind "task" เหมือน "event" ทุกประการ พร้อมฟังก์ชัน `mapExtractedToTask()` แปลง field ผลลัพธ์ดิบเป็น `TaskInput` prefill (`date`→`dueDate`, `startTime`→`dueTime`) — ยืนยันแล้วผ่าน Vite dev server จริง: `npx tsc --noEmit` และ `npm run build` ผ่านสะอาดทั้งคู่; ทดสอบใน browser พบว่าเลือก "งาน (Task)" ใน Quick Capture ขึ้น chooser ถูกต้อง, เลือก "สแกนจากรูปภาพ" ตอนยังไม่ signed in ขึ้นข้อความบังคับ sign in ที่แก้ใหม่ถูกต้อง, กด "กรอกฟอร์มเองแทน" เปิด `TaskFormModal` โหมด quickCapture พร้อมฟิลด์ "สถานที่" ว่างถูกต้อง, สร้าง Task ผ่านฟอร์มปกติพร้อมกรอกสถานที่ "ห้องประชุม A301" แล้วเปิดแก้ไขซ้ำพบว่าค่ายังอยู่ครบถูกต้อง — ยังไม่ได้ยืนยันเส้นทาง sign-in-then-scan แบบเต็มและ endpoint `/api/smart-capture` จริง เพราะติดบล็อกเดียวกับ Sprint 13 (`GEMINI_API_KEY` ยังไม่ได้ตั้งค่าบน Vercel) — อัปเดตสถานะใน `backlog.md`/`CLAUDE.md` เป็น "โค้ดเสร็จแล้ว แต่ยังไม่ผ่าน Gate 15" ยังไม่ commit/push รอ user ยืนยันตามธรรมเนียมเดิม
