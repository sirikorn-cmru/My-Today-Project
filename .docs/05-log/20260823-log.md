# Log 20260823

- ตรวจสอบ backlog.md ผ่าน backlog-sync-check: พบว่ารายการ Sprint 8 (Universal Inbox + Quick Capture, spec 009) ยังระบุสถานะ "ยังไม่เริ่ม" ทั้งที่ตรวจโค้ดจริงพบหลักฐาน build ครบตาม scope (src/components/QuickCaptureModal.tsx, src/pages/InboxPage.tsx, src/hooks/useNotes.ts, src/hooks/useLinks.ts, src/types.ts มี Note/Link/InboxKind/inInbox) และมี commit "508e98d Implement Sprint 8: Universal Inbox + Quick Capture" ขึ้น git log แล้ว — แก้ไข backlog.md ให้สถานะเป็น "เสร็จแล้ว" พร้อมหมายเหตุอ้างอิง commit และหลักฐานไฟล์
- อัปเดต feature-list.md ให้สถานะ Sprint 8 เป็น "เสร็จแล้ว" ทั้งในตารางสรุปและ subsection รายละเอียด (คง MoSCoW = Should และเหตุผลเดิมไว้ไม่เปลี่ยน) พร้อมปรับ framing ในย่อหน้าเปิดเอกสารเป็น Sprint 1-8 เสร็จแล้ว/Sprint 9-11 ยังไม่เริ่ม
- อัปเดต user-journey-student.md และ user-journey-general-person.md: ย้าย node ขั้นตอน Quick Capture/Inbox/จัดเข้า Life Area (steps 1-3 ของทั้งสอง persona) จากสไตล์ `planned` (เส้นประ) เป็น `done` (เส้นทึบ) ในไดอะแกรม Mermaid, ตัดป้าย "(แผนในอนาคต)" ออกจาก node เหล่านั้น, อัปเดตสถานะในรายการขั้นตอนเป็น "เสร็จแล้ว", และปรับหมายเหตุสถานะท้ายเอกสารเป็นวันที่ 20260823 (Sprint 1-8 เสร็จแล้ว เหลือ Sprint 9-10 ที่ยังไม่เริ่ม)
- อัปเดต `.docs/02-design/02-technical/architecture.md` สะท้อน Sprint 8 (Universal Inbox + Quick Capture) เสร็จแล้ว: ย้าย Note/Link จาก "แผนในอนาคต" เป็น entity ที่ build จริงแล้วในหัวข้อ 3 (Core Domain Concepts) พร้อมเพิ่มคำอธิบายแนวคิด "organization state" ที่ Application/Domain Logic Layer เป็นเจ้าของ (หัวข้อ 2 และ 3), ปรับสถานะขั้นตอน Quick Capture/Inbox/จัดเข้า Life Area (rows 1-3 ของทั้งสอง persona) ในหัวข้อ 4 เป็น "เสร็จแล้ว", ตัด known gaps ของ Sprint 8 ออกจากหัวข้อ 6 (เหลือเฉพาะ Sprint 9-10), และเพิ่มบรรทัด change log ลงวันที่ 20260823
- สร้าง `.docs/02-design/02-technical/database-schema.md` ครั้งแรก: ER diagram เชิงตรรกะ (conceptual, ไม่ผูก SQL/ORM/Web Storage API) ครอบคลุม Life Area, Task, Event, File, Note, Link, Personal Profile, และ Notification Read State (lookup entity เก็บสถานะอ่านแล้ว เพราะ Notification เองไม่ใช่ entity ที่เก็บถาวร) พร้อมรายละเอียดฟิลด์ต่อ entity, กฎทางธุรกิจที่กระทบ schema, ฟิลด์ข้ามระบบ (`lifeAreaId`/`inInbox`), known gaps จาก Sprint 9-10 (`linkedNoteIds`/`linkedLinkIds`/`reminderLeadTime`), และหมายเหตุความไม่สอดคล้องเล็กน้อยที่พบใน spec Sprint 10 (อ้างถึง `Task.linkedFileIds` ที่ไม่มีอยู่จริง — ความสัมพันธ์ File↔Task ปัจจุบันเก็บทิศทางเดียวที่ File)
- สร้าง `.docs/02-design/02-technical/api-spec.md` ครั้งแรก: เอกสาร operation contract เชิงแนวคิดของ Domain Logic Layer (ไม่ใช่ REST/GraphQL spec เพราะไม่มี backend) ครอบคลุม operation ต่อ entity ทั้ง 7 กลุ่ม, cross-cutting operations (Delete Life Area แบบ cascade-safe, Quick Capture, Organize from Inbox, List Inbox Items), derived/read-only operations (Build Notifications, Get Day Items, Get Today Dashboard Summary ฯลฯ), known gaps จาก Sprint 9-10 (Timeline/Smart Priority/Life Progress, Link Note/Link to Task/Event, Custom Reminder Lead Time), และหมายเหตุความไม่สอดคล้องเดียวกันกับ database-schema.md เรื่อง `linkedFileIds`
- แก้ไขสเปก Sprint 10 (`.docs/01-requirements/01-spec/20260806-011-my-today-sprint10-task-event-file-linking.md`): เพิ่ม section "เพิ่มเติม (20260823)" ท้ายไฟล์ แก้ไขความเข้าใจผิดใน Business Rule ข้อ 1 ที่อ้างถึง `Task.linkedFileIds` ซึ่งไม่มีอยู่จริง — ชี้แจงว่า Task↔File จาก Sprint 4 เก็บความสัมพันธ์ที่ฝั่ง File ผ่าน `linkedTaskIds` ไม่ใช่ field บน Task และยืนยันว่า scope/business rule อื่นของ Sprint 10 ไม่เปลี่ยนแปลง; อัปเดต `backlog.md` เพิ่มหมายเหตุการแก้ไขสเปกในรายการ Sprint 10 (สถานะการพัฒนายังคง "ยังไม่เริ่ม")
- อัปเดต `database-schema.md` และ `api-spec.md` (หัวข้อ 4 Known Gaps ของทั้งสองไฟล์): แก้หมายเหตุ "ข้อควรระวังเรื่องความสอดคล้องของ spec" จากคำแนะนำเชิง forward-looking เป็นบันทึกแบบ resolved อ้างอิง commit `83a38ee` ที่แก้ไข spec Sprint 10 แล้ว ยืนยันว่า schema/operation contract เดิมถูกต้องมาตั้งแต่แรก ไม่ต้องแก้ไขเพิ่ม พร้อมเพิ่มบรรทัด Change Log ลงวันที่ 20260823 (อัปเดตภายหลัง) ในทั้งสองไฟล์
- สร้าง `.docs/02-design/02-technical/detailed-design.md` ครั้งแรก: เอกสาร Detailed Design เชิงแนวคิด (conceptual, stack-agnostic) ระดับ sequence-flow ต่อยอดจาก architecture.md/api-spec.md/database-schema.md — ครอบคลุม sequence diagram ของทั้งสอง persona journey (นักศึกษา/บุคคลทั่วไป, เฉพาะขั้นตอนที่ build เสร็จแล้วตาม journey docs), sequence diagram ของ cross-cutting operations ทั้งสี่ (Delete Life Area แบบ two-phase, Quick Capture, Organize from Inbox, List Inbox Items), error/edge-case notes แบบ prose ใต้แต่ละไดอะแกรม, และ known gaps จาก Sprint 9-10 (Timeline/Smart Priority, Life Progress, Task/Event↔Note/Link linking, Custom Reminder Lead Time, unified Detail view) ที่ยังไม่มี diagram เพราะยังไม่ build — ไม่พบข้อขัดแย้งกับ architecture.md/api-spec.md/database-schema.md
- สร้าง `.docs/02-design/02-technical/tech-stack.md` ครั้งแรก: เอกสาร Technology Stack Decision แบบ retroactive validation ของ stack ที่ deploy จริง Sprint 1-8 (ตรวจสอบกับ `package.json`/`vite.config.ts`/`tailwind.config.js`/`tsconfig.json` แล้วสอดคล้องกันทุกจุด ไม่พบความขัดแย้ง) สรุป stack: React 18.3 + TypeScript 5.5 (strict) + Vite 5.3 + React Router 7 + Tailwind CSS 3.4, persistence LocalStorage (structured data) + IndexedDB (file blob), deploy บน Vercel free tier — เทียบกับ Vue 3 stack และ local-first-sync framework แล้วยืนยันไม่เปลี่ยนแปลง พร้อมบันทึกหัวข้อ Functional Scope และ Timeline ที่ข้ามการสัมภาษณ์เพราะซ้ำกับเอกสารอื่น/ไม่มีผลต่อการตรวจสอบย้อนหลัง
- อัปเดต `.docs/02-design/02-technical/architecture.md` (หัวข้อ 2 Container View เท่านั้น): แยกหมายเหตุ "หมายเหตุการ implement ปัจจุบัน" จากก้อนเดียวรวมเป็นหมายเหตุย่อยต่อ container พร้อมชื่อ/เวอร์ชันเทคโนโลยีที่แม่นยำขึ้น (React 18.3.1, Vite 5.3.1, TypeScript 5.5.2 strict, React Router 7.18.2, Tailwind CSS 3.4.4, LocalStorage, IndexedDB, Vercel free tier + `vercel.json`) โดยอ้างอิงจาก `tech-stack.md` ที่เพิ่งสร้าง — ไม่แตะเนื้อหาเชิงแนวคิดของหัวข้อ 1/3/4/5/6 และไม่เปลี่ยน container/diagram ใดๆ
- อัปเดต `database-schema.md` และ `api-spec.md` เพิ่มเติมอีกครั้ง (เฉพาะหมายเหตุปิดท้าย "หมายเหตุการ implement ปัจจุบัน" เท่านั้น ไม่แตะ ER diagram/ตารางฟิลด์/operation contract/known gaps): เพิ่มลิงก์ [[tech-stack|tech-stack]] ในส่วนหัวเอกสารทั้งสองไฟล์, ขยายหมายเหตุปิดท้ายของ `database-schema.md` ให้ระบุเหตุผลของการแยก LocalStorage/IndexedDB โดยอ้างอิงคำพูดตรงจาก `tech-stack.md` หัวข้อ 4 (Trade-offs) เรื่อง quota ~5-10MB string-only, และขยายหมายเหตุปิดท้ายของ `api-spec.md` ให้ผูกเหตุผล "local function call ไม่ใช่ network call" กับ Fixed Constraints/state-ownership ของ `tech-stack.md` หัวข้อ 1/3 โดยตรง พร้อมระบุเวอร์ชัน React/React Router/Vite ที่เกี่ยวข้อง เพิ่มบรรทัด Change Log ลงวันที่ 20260823 (อัปเดตภายหลังอีกครั้ง) ในทั้งสองไฟล์
- อัปเดต `.docs/02-design/02-technical/detailed-design.md` เพิ่มหัวข้อใหม่ "5. หมายเหตุการ implement ปัจจุบัน" (เดิมเอกสารนี้ไม่มีกลไก implementation-footnote เลย) โดยอ้างอิง `tech-stack.md` ที่เพิ่งสร้าง ระบุฟังก์ชัน/component/ไฟล์จริงที่ implement แต่ละ participant ของทุก sequence diagram ในหัวข้อ 1 (ทั้งสอง persona journey) และหัวข้อ 2 (Delete Life Area, Quick Capture, Organize from Inbox, List Inbox Items) พร้อมเลื่อน Change Log เดิมเป็นหัวข้อ 6 — ไม่แตะเนื้อหา sequence diagram/error-edge-case notes/known gaps เดิมในหัวข้อ 1-4
- อัปเดต `.docs/02-design/02-technical/architecture.md` แบบ targeted อีกครั้ง (ไม่แตะหัวข้อ 1-4): เพิ่ม cross-link ไปยัง [[../../01-requirements/01-spec/20260823-013-my-today-non-functional-requirements-master|NFR master list]] ที่เพิ่งสร้างในบรรทัด "เชื่อมโยงกลับ" ของหัวข้อบน, เพิ่มบรรทัดชี้ในหัวข้อ 5 (Cross-cutting Architectural Principles) ว่าชุด NFR แบบเต็มอยู่ที่เอกสารนั้น, และเพิ่ม known gap ใหม่ 2 รายการในหัวข้อ 6 (Accessibility Baseline กระทบ Presentation/Interaction Layer, IndexedDB Quota-Warning กระทบ Binary/Blob Local Persistence — ไม่รวม Browser Compatibility Matrix เพราะเป็นขอบเขตการทดสอบ ไม่ใช่ gap เชิงสถาปัตยกรรม) พร้อมบรรทัด Change Log ใหม่ลงวันที่ 20260823
- สร้างสเปกใหม่ `.docs/01-requirements/01-spec/20260823-013-my-today-non-functional-requirements-master.md` (running no. 013): เอกสาร Non-Functional Requirements Master List — สังเคราะห์ NFR ที่กระจัดกระจายอยู่แล้วใน Sprint 1/4/5/6/7/8/9/11, `tech-stack.md`, `architecture.md` มารวมเป็น 9 หมวด (Performance, Reliability/Data Integrity, Usability/UX, Accessibility, Security/Privacy/Compliance, Compatibility/Portability, Offline Capability, Scalability/Capacity, Maintainability) พร้อมอ้างอิงแหล่งที่มาต่อหมวดแบบเดียวกับที่ doc 008 อ้างอิง Sprint ต่อ FR และเพิ่มข้อกำหนดใหม่ 3 ข้อที่ไม่เคยมีเอกสารใดระบุมาก่อน — Accessibility Baseline (semantic HTML/keyboard focus/contrast อ่านง่าย ไม่ใช่ WCAG AA เต็มรูปแบบ), Browser Compatibility Matrix (Chrome/Edge/Firefox 2 เวอร์ชันล่าสุด, Safari ล่าสุด, Mobile Chrome/Safari ล่าสุด), IndexedDB Quota-Warning (แจ้งเตือนผู้ใช้เมื่อพื้นที่ไฟล์แนบใกล้เต็ม ยังไม่กำหนดตัวเลข %) — ระบุชัดว่า 3 ข้อนี้ยังไม่ผูกกับ Sprint ใดและยังไม่เริ่มพัฒนา พร้อมหมายเหตุว่า `.docs/03-testing/` ยังไม่ active จึงยังไม่มี test case verify NFR เหล่านี้; อัปเดต `.docs/01-requirements/backlog.md` เพิ่มรายการอ้างอิงเอกสารนี้ (สถานะ "ยังไม่เริ่ม" เฉพาะ 3 หัวข้อย่อยใหม่ ส่วน 6 หมวดที่เหลือถือว่า Sprint ที่เกี่ยวข้อง implement ไปแล้ว)
- อัปเดต `database-schema.md` และ `api-spec.md` แบบ targeted อีกครั้ง (ตอบสนอง NFR-08 IndexedDB Quota-Warning จาก spec 013 เท่านั้น ไม่แตะ ER diagram/ตารางฟิลด์/ฟิลด์ข้ามระบบ/operation contract/cross-cutting/derived operations เดิม): เพิ่ม cross-link ไปยัง [[../../01-requirements/01-spec/20260823-013-my-today-non-functional-requirements-master|NFR master list]] ในบรรทัดเชื่อมโยงกลับของทั้งสองไฟล์; เพิ่ม known gap ใหม่ในหัวข้อ 4 ของ `database-schema.md` ชี้แจงว่าไม่ต้องมีฟิลด์ schema ใหม่บน `File` (เป็น query ความจุของกลไกจัดเก็บเอง ไม่ใช่ per-record data); เพิ่ม known gap ใหม่ในหัวข้อ 4 ของ `api-spec.md` คือ operation "Get Storage Usage Estimate / Warn Near Quota" (derived/read-only, query ความจุคงเหลือของ Binary/Blob Local Persistence container) พร้อมบรรทัด Change Log ใหม่ลงวันที่ 20260823 ในทั้งสองไฟล์
- อัปเดต `.docs/02-design/02-technical/detailed-design.md` แบบ targeted อีกครั้ง (ตอบสนอง NFR-08 IndexedDB Quota-Warning จาก spec 013 เพื่อให้สอดคล้องกับ known gap ที่เพิ่งเพิ่มใน `api-spec.md`/`database-schema.md`/`architecture.md` เท่านั้น ไม่แตะ sequence diagram ในหัวข้อ 1/2, error/edge-case notes หัวข้อ 3, หรือหมายเหตุ implement หัวข้อ 5): เพิ่ม cross-link ไปยัง [[../../01-requirements/01-spec/20260823-013-my-today-non-functional-requirements-master|NFR master list]] ในบรรทัดเชื่อมโยงกลับ; เพิ่มประโยคใน §4 Known Gaps สำหรับ IndexedDB Quota-Warning (NFR-08) ระบุว่า operation "Get Storage Usage Estimate / Warn Near Quota" ยังไม่ build จึงยังไม่มี sequence diagram ให้ พร้อมบรรทัด Change Log ใหม่ลงวันที่ 20260823 (อัปเดตภายหลังอีกครั้ง)
- แก้ไขสเปก Sprint 11 (`.docs/01-requirements/01-spec/20260806-012-my-today-sprint11-competition-demo-freeze.md`): เพิ่ม section "เพิ่มเติม (20260823)" ท้ายไฟล์ ผนวก 3 หัวข้อย่อย NFR จาก [[01-spec/20260823-013-my-today-non-functional-requirements-master|NFR Master List]] ที่ยังไม่เคยผูกกับ Sprint ใดเข้า scope ของ Sprint 11 — Accessibility Baseline (NFR-04) และ Browser Compatibility Matrix (NFR-06) ขยายขอบเขตจาก "เฉพาะหน้าจอใหม่ Sprint 7-10" เป็นครอบคลุมทั้งระบบ Sprint 1-10, และ IndexedDB Quota-Warning (NFR-08) เพิ่มเป็นงานใหม่โดยได้รับยกเว้นจาก Business Rule ห้ามเพิ่ม Feature ใหม่ (อ้างอิง pattern เดียวกับ Privacy Notice ของ Sprint 6); ไม่แก้ไข/ลบ section เดิมใดๆ ของสเปก; อัปเดต `backlog.md` ทั้งรายการ Sprint 11 (012, เพิ่มหมายเหตุการผนวก NFR) และรายการ NFR Master List (013, ระบุว่า NFR-04/06/08 ผูกกับ Sprint 11 แล้ว ไม่ใช่ "ไม่ผูกกับ Sprint ใด" อีกต่อไป) — สถานะการพัฒนาของ Sprint 11 ยังคง "ยังไม่เริ่ม"
- สร้างเอกสารแรกใน `.docs/03-testing/01-test-plan/`: `20260823-nfr-test-plan.md` — Test Plan สำหรับ NFR ทั้ง 9 หมวดตาม NFR Master List (013) แบ่งเป็น Test Case ที่ทดสอบได้ทันที (Performance/Reliability/Usability/Security/Offline/Maintainability — 6 หมวดที่ implement แล้ว) และ Test Case ที่ต้องรอ Sprint 11 build (Accessibility Baseline/Browser Compatibility Matrix/IndexedDB Quota-Warning — 3 หัวข้อย่อยใหม่ที่เพิ่งผูก Sprint) พร้อมอ้างอิงกลับไปยัง Sprint 11 spec's "เพิ่มเติม (20260823)" section — เป็นเอกสารแรกที่ทำให้ `03-testing/` เริ่ม active
- อัปเดต `.docs/01-requirements/feature-list.md` แบบ targeted อีกครั้ง (ผ่าน `feature-journey-sync`): แก้เฉพาะ Sprint 11 (แถวตารางสรุป + subsection รายละเอียด "คำอธิบาย") สะท้อนสเปก Sprint 11 ที่เพิ่งผนวก NFR-04/06/08 จาก [[01-spec/20260823-013-my-today-non-functional-requirements-master|NFR Master List]] เข้า scope (commit `d6874c3`) — Accessibility Baseline และ Browser Compatibility Matrix ขยายจาก "เฉพาะหน้าจอใหม่ Sprint 7-10" เป็นครอบคลุมทั้งระบบ Sprint 1-10, IndexedDB Quota-Warning เป็นฟีเจอร์ใหม่ที่ได้รับยกเว้นจาก Business Rule ห้ามเพิ่ม Feature ใหม่ (อ้าง precedent Privacy Notice ของ Sprint 6); ไม่เปลี่ยน MoSCoW/เหตุผลเดิมของ Sprint 11 (ยังคง Must), ไม่แตะ Sprint อื่นใด, คอลัมน์ FR ที่เกี่ยวข้อง, หรือ `user-journey-student.md`/`user-journey-general-person.md`; เพิ่มบรรทัด Change Log ลงวันที่ 20260823 ท้ายเอกสาร
- แก้ inconsistency แบบ targeted ใน `.docs/02-design/02-technical/architecture.md` หัวข้อ 6 เท่านั้น (ไม่แตะหัวข้อ 1-5 หรือ diagram): เพิ่มป้ายอ้างอิง "(Sprint 11)" ให้ bullet Accessibility Baseline และ IndexedDB Quota-Warning เพื่อสะท้อนว่า Sprint 11 ผนวกทั้งสองรายการเข้า scope แล้ว (commit `d6874c3` แก้สเปก Sprint 11) และแก้บรรทัดปิดท้ายที่เคยระบุผิดว่า "Sprint 11 ไม่เพิ่ม container หรือ flow ใหม่ จึงไม่มีรายการเพิ่มในหัวข้อนี้" ให้ถูกต้องว่า Sprint 11 ไม่เพิ่ม container ใหม่ แต่เพิ่มพฤติกรรมใหม่ให้ 2 container เดิม (Presentation/Interaction Layer สำหรับ Accessibility Baseline, Binary/Blob Local Persistence สำหรับ IndexedDB Quota-Warning) ผ่าน 2 bullet ที่มีอยู่แล้ว โดยไม่นับ Browser Compatibility Matrix เพราะเป็นขอบเขตการทดสอบ; เพิ่มบรรทัด Change Log ใหม่ลงวันที่ 20260823
- แก้ citation ให้สอดคล้องกัน (targeted, 1 บรรทัด/ไฟล์) ใน `database-schema.md` หัวข้อ 4 และ `api-spec.md` หัวข้อ 4: เพิ่ม "Sprint 11" เข้าไปใน citation ของ known gap "IndexedDB Quota-Warning" / "Get Storage Usage Estimate / Warn Near Quota" (จาก "(NFR-08, ...)" เป็น "(Sprint 11, NFR-08, ...)") ให้ตรงกับ commit `9ee5476` ที่เพิ่งแก้ architecture.md หัวข้อ 6 ในลักษณะเดียวกัน; ไม่แตะ ER diagram/ตารางฟิลด์/ฟิลด์ข้ามระบบ/operation contract/cross-cutting/derived operations/หมายเหตุ Sprint 10 linkedFileIds ใดๆ ในทั้งสองไฟล์ พร้อมเพิ่มบรรทัด Change Log ใหม่ลงวันที่ 20260823 ในทั้งสองไฟล์
- แก้ inconsistency แบบ targeted ใน `.docs/02-design/02-technical/detailed-design.md` หัวข้อ 4 เท่านั้น (ไม่แตะหัวข้อ 1/2/3/5 หรือ bullet อื่นใน §4): เพิ่มป้ายอ้างอิง "(Sprint 11, NFR-08, ...)" ให้ bullet "IndexedDB Quota-Warning" (เดิมมีเฉพาะ "(NFR-08)") ให้ตรงกับ `api-spec.md`/`database-schema.md` ที่เพิ่งแก้ในลักษณะเดียวกัน (commit `9ee5476`, `2077b73`) และแก้บรรทัดปิดท้าย §4 ที่เคยระบุผิดว่า "Sprint 11 ไม่เพิ่ม flow ใหม่ จึงไม่มีรายการเพิ่มในหัวข้อนี้" — เนื่องจากสเปก Sprint 11 (`20260806-012-...`) ถูกแก้ในคอมมิต `d6874c3` ให้ผนวก IndexedDB Quota-Warning เข้า scope ของตนแล้ว จึงแก้เป็นประโยคยืนยันว่า Sprint 11 ไม่เพิ่ม sequence diagram ใหม่ของตัวเอง แต่เป็นเจ้าของ operation ที่ยังไม่ build ("Get Storage Usage Estimate / Warn Near Quota") ตาม bullet ด้านบน; เพิ่มบรรทัด Change Log ใหม่ลงวันที่ 20260823
- **บันทึกอ้างอิงแบบไม่เป็นทางการ (ไม่ใช่ spec/tech-stack doc)** — ทำ Weighted Scoring Model เปรียบเทียบ 5 ตัวเลือก Frontend+Backend stack สำหรับ "Daily Orchestrator" AI phase ที่คาดว่าจะเกิดขึ้นหลัง Sprint 11 Freeze (ยังไม่มี requirement ที่เป็นรูปธรรมสำหรับ phase นี้ ตามที่ `tech-stack.md` §5 ระบุไว้แล้วว่าเป็น open risk) เกณฑ์ถ่วงน้ำหนักอิงจาก NFR Master List (013) และ interview เดิมใน `tech-stack.md`: Team fit (25), Cost/free-tier (20), AI integration ease (20), Migration effort (15), Offline compat (10), Ops simplicity (10) — ตัวเลือกที่ประเมิน: (1) React+Vite เดิม + Vercel Serverless Functions **คะแนนสูงสุด 500/500 (แนะนำ)**, (2) React+Vite เดิม + Cloudflare Workers+D1 (450), (3) Migrate เป็น Next.js (400), (4) React+Vite เดิม + Supabase (365), (5) React+Vite เดิม + Python/FastAPI (300) — **คำแนะนำ: ยังไม่สร้างเอกสารทางการ** จนกว่าจะมี `requirement-intake` กำหนด scope ที่เป็นรูปธรรมของ Daily Orchestrator phase ก่อน แล้วค่อยรัน `tech-stack-advisor` แบบเต็มรูปแบบโดยอิงข้อมูลจากตารางนี้เป็นจุดเริ่มต้น ไม่ใช่ข้อสรุปสุดท้าย
