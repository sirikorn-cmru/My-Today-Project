# Log 20260823

- ตรวจสอบ backlog.md ผ่าน backlog-sync-check: พบว่ารายการ Sprint 8 (Universal Inbox + Quick Capture, spec 009) ยังระบุสถานะ "ยังไม่เริ่ม" ทั้งที่ตรวจโค้ดจริงพบหลักฐาน build ครบตาม scope (src/components/QuickCaptureModal.tsx, src/pages/InboxPage.tsx, src/hooks/useNotes.ts, src/hooks/useLinks.ts, src/types.ts มี Note/Link/InboxKind/inInbox) และมี commit "508e98d Implement Sprint 8: Universal Inbox + Quick Capture" ขึ้น git log แล้ว — แก้ไข backlog.md ให้สถานะเป็น "เสร็จแล้ว" พร้อมหมายเหตุอ้างอิง commit และหลักฐานไฟล์
- อัปเดต feature-list.md ให้สถานะ Sprint 8 เป็น "เสร็จแล้ว" ทั้งในตารางสรุปและ subsection รายละเอียด (คง MoSCoW = Should และเหตุผลเดิมไว้ไม่เปลี่ยน) พร้อมปรับ framing ในย่อหน้าเปิดเอกสารเป็น Sprint 1-8 เสร็จแล้ว/Sprint 9-11 ยังไม่เริ่ม
- อัปเดต user-journey-student.md และ user-journey-general-person.md: ย้าย node ขั้นตอน Quick Capture/Inbox/จัดเข้า Life Area (steps 1-3 ของทั้งสอง persona) จากสไตล์ `planned` (เส้นประ) เป็น `done` (เส้นทึบ) ในไดอะแกรม Mermaid, ตัดป้าย "(แผนในอนาคต)" ออกจาก node เหล่านั้น, อัปเดตสถานะในรายการขั้นตอนเป็น "เสร็จแล้ว", และปรับหมายเหตุสถานะท้ายเอกสารเป็นวันที่ 20260823 (Sprint 1-8 เสร็จแล้ว เหลือ Sprint 9-10 ที่ยังไม่เริ่ม)
- อัปเดต `.docs/02-design/02-technical/architecture.md` สะท้อน Sprint 8 (Universal Inbox + Quick Capture) เสร็จแล้ว: ย้าย Note/Link จาก "แผนในอนาคต" เป็น entity ที่ build จริงแล้วในหัวข้อ 3 (Core Domain Concepts) พร้อมเพิ่มคำอธิบายแนวคิด "organization state" ที่ Application/Domain Logic Layer เป็นเจ้าของ (หัวข้อ 2 และ 3), ปรับสถานะขั้นตอน Quick Capture/Inbox/จัดเข้า Life Area (rows 1-3 ของทั้งสอง persona) ในหัวข้อ 4 เป็น "เสร็จแล้ว", ตัด known gaps ของ Sprint 8 ออกจากหัวข้อ 6 (เหลือเฉพาะ Sprint 9-10), และเพิ่มบรรทัด change log ลงวันที่ 20260823
- สร้าง `.docs/02-design/02-technical/database-schema.md` ครั้งแรก: ER diagram เชิงตรรกะ (conceptual, ไม่ผูก SQL/ORM/Web Storage API) ครอบคลุม Life Area, Task, Event, File, Note, Link, Personal Profile, และ Notification Read State (lookup entity เก็บสถานะอ่านแล้ว เพราะ Notification เองไม่ใช่ entity ที่เก็บถาวร) พร้อมรายละเอียดฟิลด์ต่อ entity, กฎทางธุรกิจที่กระทบ schema, ฟิลด์ข้ามระบบ (`lifeAreaId`/`inInbox`), known gaps จาก Sprint 9-10 (`linkedNoteIds`/`linkedLinkIds`/`reminderLeadTime`), และหมายเหตุความไม่สอดคล้องเล็กน้อยที่พบใน spec Sprint 10 (อ้างถึง `Task.linkedFileIds` ที่ไม่มีอยู่จริง — ความสัมพันธ์ File↔Task ปัจจุบันเก็บทิศทางเดียวที่ File)
- สร้าง `.docs/02-design/02-technical/api-spec.md` ครั้งแรก: เอกสาร operation contract เชิงแนวคิดของ Domain Logic Layer (ไม่ใช่ REST/GraphQL spec เพราะไม่มี backend) ครอบคลุม operation ต่อ entity ทั้ง 7 กลุ่ม, cross-cutting operations (Delete Life Area แบบ cascade-safe, Quick Capture, Organize from Inbox, List Inbox Items), derived/read-only operations (Build Notifications, Get Day Items, Get Today Dashboard Summary ฯลฯ), known gaps จาก Sprint 9-10 (Timeline/Smart Priority/Life Progress, Link Note/Link to Task/Event, Custom Reminder Lead Time), และหมายเหตุความไม่สอดคล้องเดียวกันกับ database-schema.md เรื่อง `linkedFileIds`
- แก้ไขสเปก Sprint 10 (`.docs/01-requirements/01-spec/20260806-011-my-today-sprint10-task-event-file-linking.md`): เพิ่ม section "เพิ่มเติม (20260823)" ท้ายไฟล์ แก้ไขความเข้าใจผิดใน Business Rule ข้อ 1 ที่อ้างถึง `Task.linkedFileIds` ซึ่งไม่มีอยู่จริง — ชี้แจงว่า Task↔File จาก Sprint 4 เก็บความสัมพันธ์ที่ฝั่ง File ผ่าน `linkedTaskIds` ไม่ใช่ field บน Task และยืนยันว่า scope/business rule อื่นของ Sprint 10 ไม่เปลี่ยนแปลง; อัปเดต `backlog.md` เพิ่มหมายเหตุการแก้ไขสเปกในรายการ Sprint 10 (สถานะการพัฒนายังคง "ยังไม่เริ่ม")
