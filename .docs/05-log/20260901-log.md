# Log 20260901

- **Sprint 12 Cloud Sync — deploy จริงบน Vercel + ยืนยัน multi-device sync จริงสำเร็จโดย user เอง:** user deploy โปรเจกต์ขึ้น Vercel สำเร็จ (`https://my-today-project.vercel.app`) เชื่อม repo `sirikorn-cmru/My-Today-Project` เข้ากับ Vercel project ใหม่ — ตรวจสอบแล้วว่าโหลดถูกต้อง ไม่มี console error, ข้อมูล seed แสดงผลปกติ และ Google Sign-In ทำงานถูกต้องจาก domain จริง (ทดสอบกดปุ่ม Sign-In จริงในเบราว์เซอร์ พบว่า redirect ผ่าน `my-today-a25d9.firebaseapp.com` ไปถึงหน้า Sign in ของ Google จริง — ยืนยันว่า Vercel domain ใช้งานได้ทันทีโดยไม่ต้องเพิ่ม authorized domain เพิ่มเติมใน Firebase Console)
- **User ทดสอบ multi-device sync จริงด้วยตัวเอง (สำเร็จ):** sign in ด้วยบัญชี Google จริงที่ `/profile` บน URL ที่ deploy จริง, เปิด Cloud Sync, เพิ่ม Task ใหม่ในเบราว์เซอร์ปกติ — จากนั้นเปิด Incognito window แยกต่างหาก (จำลอง "อุปกรณ์ใหม่" ที่ไม่มีข้อมูล local เดิมเลย) sign in ด้วยบัญชีเดียวกัน เปิด Cloud Sync — **Task เดิมปรากฏขึ้นเองทันทีโดยไม่ต้องพิมพ์ใหม่** ยืนยัน Acceptance Criteria ของ [[../01-requirements/01-spec/20260829-014-my-today-sprint12-cloud-sync|Sprint 12 spec]] ("สร้าง/แก้/ลบ Task บนอุปกรณ์ A แล้วเปิดแอป login เดียวกันบนอุปกรณ์ B เห็นข้อมูลเดียวกัน") ผ่านจริงแล้วด้วยหลักฐานจริง ไม่ใช่แค่ตรวจโค้ด — ระหว่างทาง user เจอหน้า "Enable MFA" ตอนพยายามเข้า Firebase Console เพื่อดู Firestore data ตรงๆ (Google บังคับให้เปิด 2-Step Verification ก่อนเข้า Console ได้ — เป็นเรื่อง account security ของ user เอง ไม่เกี่ยวกับแอป) จึงข้ามไปพิสูจน์ผ่านการทดสอบ pull-and-merge ข้าม session แทน ซึ่งพิสูจน์ผลลัพธ์เดียวกันได้แน่นหนากว่าด้วยซ้ำ (เห็นผลจริงของการ sync ไม่ใช่แค่เห็นข้อมูลนิ่งๆ ใน Console)
- **backlog.md:** อัปเดต Sprint 12 entry — ย้าย "การ login จริงแบบเต็มรูปแบบ + multi-device sync จริง" จากรายการ "ยังไม่ได้ยืนยัน" ไปเป็น "แก้ไขแล้ว" พร้อมเพิ่มบรรทัดยืนยันการ deploy บน Vercel สำเร็จ, เพิ่มหมายเหตุ checkpoint 20260901 ที่หัวรายการ Sprint 12 — สถานะยังคง "กำลังดำเนินการ" (ไม่เปลี่ยนเป็น "เสร็จแล้ว") เพราะเหลือแค่ Security Rules Emulator test (ต้องการ Java 21+) เท่านั้นก่อน Gate 12 จะผ่านครบ — ยังไม่ commit/push รอ user ยืนยันตามธรรมเนียมเดิมของโปรเจกต์นี้
