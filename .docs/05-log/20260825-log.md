# Log 20260825

- **backlog-sync-check:** ตรวจสอบ `backlog.md` เทียบกับ 13 spec doc ใน `01-spec/` และโค้ดจริงใน `src/` — coverage ครบ 13/13 ไม่มี spec doc ไหนขาด entry และไม่พบ wikilink เสีย (รวม `[[../00-archived/20260806-001-my-today-sprint1-mvp|001, archived]]` ก็ยังชี้ถูกที่). พบว่าสถานะ Sprint 11 เดิม "ยังไม่เริ่ม" ไม่ตรงกับความจริงอีกต่อไป — มีความคืบหน้าโค้ดบางส่วนใน working tree (ยังไม่ commit): IndexedDB Quota-Warning (NFR-08, `src/lib/storageQuota.ts` ใหม่ + `useFiles.ts`/`FilesPage.tsx`) เสร็จสมบูรณ์, Accessibility Baseline (NFR-04) ส่วน semantic HTML เสร็จ (เพิ่ม `<main>` landmark ครบ 10 หน้า), cleanup mock/debug code เสร็จ (ไม่พบสิ่งที่ต้องลบ), integration smoke-check เบื้องต้นผ่าน — แต่ Browser Compatibility Matrix (NFR-06), Competition Demo script, Black Box Testing เป็นทางการ, และ Gate 11 ยังไม่ได้ทำ จึงแก้สถานะ Sprint 11 เป็น **"กำลังดำเนินการ"** (สถานะกลางที่ไม่เคยใช้มาก่อนในไฟล์นี้) พร้อมหมายเหตุแจกแจงรายการที่เสร็จ/ยังไม่เสร็จอย่างละเอียด และระบุชัดว่ายังไม่มี commit ขึ้น git log สำหรับงานส่วนนี้
- **feature-journey-sync:** อัปเดต `feature-list.md` แบบ targeted เฉพาะ Sprint 11 (ตารางสรุป + subsection รายละเอียด) ให้สอดคล้องกับสถานะใหม่ใน `backlog.md` — เปลี่ยนสถานะจาก "ยังไม่เริ่ม" เป็น "กำลังดำเนินการ" และเพิ่มคำอธิบายสรุปสั้นๆ ว่า 2 ใน 3 หัวข้อย่อย NFR (Accessibility Baseline ส่วน semantic HTML, IndexedDB Quota-Warning) เสร็จแล้วในโค้ด (ยังไม่ commit) ส่วน Browser Compatibility Matrix, Demo script, และ Black Box Testing เป็นทางการยังค้างอยู่ก่อน Gate 11 — ไม่เปลี่ยน MoSCoW (ยังคง Must), ไม่แตะ Sprint อื่น, FR-ID column, หรือ `user-journey-student.md`/`user-journey-general-person.md` (ไม่มี step เกี่ยวข้องกับ NFR เหล่านี้)
- **architecture-doc-sync:** อัปเดต `architecture.md` หัวข้อ 6 (Known Gaps) แบบ targeted — ไม่ใช่การประกาศ Sprint 11 เสร็จแล้ว: ตัด bullet "IndexedDB Quota-Warning" ออกทั้งหมด (build เสร็จสมบูรณ์แล้ว ตรวจสอบใน browser แล้ว), แก้ wording bullet "Accessibility Baseline" ให้สะท้อนว่า build บางส่วนเท่านั้น (เฉพาะ semantic HTML `<main>` landmark ครบ 10 หน้า ส่วน keyboard-focus/contrast มีอยู่แล้วจากงานก่อนหน้าแต่ยังไม่ผ่านการตรวจสอบ/ยืนยันอย่างเป็นทางการ เช่น WCAG contrast ratio), และปรับประโยคเปิด/ปิดหัวข้อ 6 ให้สอดคล้องกับสถานะ backlog.md ใหม่ "กำลังดำเนินการ" แทน "ยังไม่เริ่ม" — เพิ่ม Change Log บรรทัดใหม่ระบุชัดว่าเป็น partial-progress update ไม่แตะหัวข้อ 1-5
- **data-api-doc-sync:** อัปเดต `database-schema.md` และ `api-spec.md` แบบ targeted เฉพาะ IndexedDB Quota-Warning (NFR-08) — ใน `database-schema.md` หัวข้อ 4 Known Gaps ตัด bullet "IndexedDB Quota-Warning" ออก (build จริงแล้ว: `src/lib/storageQuota.ts`, `useFiles.ts`, `FilesPage.tsx`) ยืนยันว่าไม่ต้องเพิ่มฟิลด์ schema ใหม่ตามที่เคยสรุปไว้; ใน `api-spec.md` ย้าย operation "Get Storage Usage Estimate / Warn Near Quota" จากหัวข้อ 4 Known Gaps ไปหัวข้อ 3 Derived/Read-only Operations พร้อม Input/Output/Notes เต็ม (pattern เดียวกับ Build Notifications) — ไม่แตะ ER diagram, ตารางฟิลด์, operation contract อื่น, หรือ known gap อื่นใดของ Sprint 10/11 เพิ่ม Change Log บรรทัดใหม่ในทั้งสองเอกสาร
- **detailed-design-doc-sync:** อัปเดต `detailed-design.md` แบบ targeted เฉพาะ IndexedDB Quota-Warning (NFR-08) เช่นกัน — **partial-progress update ของ Sprint 11 เท่านั้น ไม่ใช่การประกาศว่า Sprint 11 เสร็จทั้ง Sprint** ตาม backlog.md ที่ยังระบุสถานะ "กำลังดำเนินการ": ลบ bullet "IndexedDB Quota-Warning" ออกจาก §4 Known Gaps ทั้งหมด (ไม่มี known gap คงเหลือในหัวข้อนี้แล้ว) พร้อมอธิบายว่าส่วนที่เหลือของ Sprint 11 (Browser Compatibility Matrix, Accessibility Baseline อย่างเป็นทางการ, Demo script, Black Box Testing, Gate 11) เป็นงาน testing/polish ไม่ใช่ flow ใหม่ที่ต้องมี diagram; เพิ่ม §2.6 ใหม่ (Get Storage Usage Estimate / Warn Near Quota) เป็นคำอธิบาย flow แบบร้อยแก้วสั้นๆ **ไม่มี sequence diagram เต็มรูปแบบ** เพราะเป็น derived/read-only check ต่อความจุของ container เดียว (Binary/Blob Local Persistence) เรียบง่ายแบบเดียวกับ Build Notifications; เพิ่มหมายเหตุการ implement ใหม่ใน §5 อ้างอิง `src/lib/storageQuota.ts`, `useFiles.ts`, `FilesPage.tsx` — ไม่แตะ §1 persona diagrams, ไดอะแกรม cross-cutting operations อื่นใน §2 (2.1-2.5), หรือ §3 error/edge-case notes ใดๆ เพิ่ม Change Log บรรทัดใหม่
