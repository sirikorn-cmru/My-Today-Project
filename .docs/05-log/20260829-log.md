# Log 20260829

- **Repo cleanup — stray Claude Code worktree scratch committed by mistake:** พบว่า commit "c8e5e94 feat: add Life Areas, Notifications, Privacy, Profile, and Tasks pages..." (author: Winai Kankhat, 2026-08-29 11:07) ถูก push ขึ้น `origin/master` แล้วโดยไม่ทราบมาก่อน — ตรวจสอบ diff จริงพบว่าแม้ commit message จะบอกว่าเพิ่มหน้าต่างๆ แต่เนื้อหาจริงคือการ commit โฟลเดอร์ scratch ของ tooling เข้าไปโดยไม่ได้ตั้งใจ: `.claude/worktrees/quirky-shtern-6ff1bb/` (สำเนาซ้ำทั้งชุดของ agents/skills/docs/src จาก git worktree ที่ค้างจากการรัน Agent แบบ `isolation: worktree` ในอดีต) และ `.freebuff/project-id` (ไฟล์ config ของเครื่องมืออื่นที่ไม่เกี่ยวกับโปรเจกต์นี้) รวม 141 ไฟล์ 12,594 บรรทัด — หน้าจริงของแอป (LifeAreasPage ฯลฯ) มีอยู่แล้วจาก Sprint 7 ไม่ใช่ของใหม่จาก commit นี้ ถามผู้ใช้ก่อนดำเนินการ (ตัวเลือกที่เลือก: ลบไฟล์ scratch ออกด้วย commit ใหม่ ไม่แตะ history เดิม) แล้ว `git rm -r --cached` ทั้งสองพาธ พร้อมเพิ่ม `.claude/worktrees/` และ `.freebuff/` ใน `.gitignore` กันไม่ให้เกิดซ้ำ — commit "26099d8 Untrack stray Claude Code worktree scratch and unrelated tool config" push ขึ้น `origin/master` แล้ว ยืนยันว่า `src/pages/` ของจริงไม่ถูกแตะต้อง และ working tree สะอาด
- **UX/UI Re-verify มาตรฐาน Sprint 6 ครบทุกหน้าจอ Sprint 7-10:** ตรวจสอบตามเกณฑ์ของ [[../01-requirements/01-spec/20260806-006-my-today-sprint6-integration-ux-final-testing|Sprint 6 spec]] (Empty State/Loading State/Validation/Confirmation ก่อน Delete/Error Message) กับทุกหน้าจอ/component ที่เพิ่มใน Sprint 7-10 (`LifeAreasPage`, `ProfilePage`, `InboxPage`, `QuickCaptureModal`, `NoteFormModal`, `LinkFormModal`, `TimelinePage`, `TimelineSection`, `LifeProgress`, `TaskDetailModal`, `EventDetailModal` + ส่วน `quickCapture` ของฟอร์มเดิม) โดยอ่านโค้ดจริงทุกไฟล์แล้วทดสอบจริงในเบราว์เซอร์เมื่อพบจุดน่าสงสัย — พบช่องโหว่จริง 2 จุดและแก้ไขทันที: (1) [LifeAreasPage.tsx](../../src/pages/LifeAreasPage.tsx) แก้ไขชื่อ Life Area เป็นค่าว่างแล้วกด "บันทึก" เคยเงียบๆ ไม่มี Error Message เลย (ยืนยันจริงในเบราว์เซอร์ก่อนแก้: แก้ "Health" เป็นค่าว่างแล้วกดบันทึก ชื่อไม่เปลี่ยนแต่ไม่มีอะไรแจ้งผู้ใช้) — เพิ่ม `disabled={!editingName.trim()}` ให้ปุ่ม "บันทึก" ตรงกับ pattern เดียวกับฟอร์ม "+ เพิ่ม" เดิมในหน้าเดียวกัน, (2) [InboxPage.tsx](../../src/pages/InboxPage.tsx) ไม่เคยมี Loading State ระหว่างรอไฟล์โหลดจาก IndexedDB ต่างจาก `FilesPage` ที่มี spinner + "กำลังโหลดไฟล์..." อยู่แล้ว — เพิ่ม indicator แบบเดียวกันในแท็บ Inbox เมื่อ `!filesLoaded` ทั้งสองจุดทดสอบยืนยันในเบราว์เซอร์แล้วว่า non-regression (`npm run build` ผ่าน — `tsc --noEmit` + vite build สำเร็จ, แก้ไขชื่อ Life Area ปกติยังทำงานถูกต้อง, Inbox แสดง empty state ปกติหลังโหลดเสร็จ) พบข้อสงสัยเพิ่มอีก 1 จุด (`TaskDetailModal`/`EventDetailModal` ปุ่ม "ลบออก" ไม่มี confirm) ตรวจโค้ดแล้วยืนยันเป็นการออกแบบที่ตั้งใจ — เป็นการ unlink (ตัดความสัมพันธ์ ย้อนกลับได้ทันทีผ่าน dropdown แนบใหม่) ไม่ใช่ delete ถาวรเหมือนปุ่ม "ลบ" อื่นๆ ในระบบ ไม่ต้องแก้ไข — บันทึกผลละเอียดที่ [[../03-testing/02-test-result/20260829-ux-ui-reverify-result|20260829-ux-ui-reverify-result.md]]
- **backlog.md:** อัปเดต Sprint 11 entry แบบ targeted — ย้าย "การตรวจสอบ UX/UI มาตรฐานเดียวกับ Sprint 6" จากรายการ "ยังไม่ได้ทำ" ไปเป็นรายการ "ทำเสร็จแล้วเพิ่มเติม (ตรวจสอบ 20260829)" พร้อมสรุปช่องโหว่ที่พบ/แก้ไข 2 จุดและลิงก์ไปยังเอกสารผลทดสอบ, เพิ่มหมายเหตุ checkpoint 20260829 ที่หัวรายการ Sprint 11 — สถานะยังคง **"กำลังดำเนินการ"** เหมือนเดิม ไม่เปลี่ยนเป็น "เสร็จแล้ว" เพราะเหลือแค่ Browser Compatibility Matrix (รันข้าม browser จริง) และ Gate 11 (ซ้อม demo เต็มรอบ) เท่านั้น — ยังไม่ commit/push รอ user ยืนยันตามธรรมเนียมเดิมของโปรเจกต์นี้
- **Requirement intake ใหม่ — Sprint 12: Cloud Sync (จุดเริ่มต้นของ Version 3):** สร้างเอกสาร spec ใหม่ [[../01-requirements/01-spec/20260829-014-my-today-sprint12-cloud-sync|20260829-014-my-today-sprint12-cloud-sync.md]] ผ่านช่องทาง requirement intake ที่ Freeze rule ของ [[../01-requirements/01-spec/20260806-012-my-today-sprint11-competition-demo-freeze|Sprint 11]] อนุญาตไว้เอง (ไม่ใช่การฝ่าฝืน Freeze) — กำหนดขอบเขตการเพิ่ม Firebase Authentication (Google Sign-In) + Cloud Firestore sync แบบ opt-in เสริม local-first เดิม ครอบคลุม Task/CalendarEvent/Note/Link/LifeArea/Profile (ไม่รวม FileRecord/blob), last-write-wins conflict resolution, Security Rules ต่อ user, และปรับ Privacy Notice ให้ครอบคลุมสถานะ sync เปิด/ปิด — เพิ่มแถวใหม่ใน `backlog.md` (สถานะ: ยังไม่เริ่ม) ลิงก์กลับไปยัง spec ไฟล์นี้
- **สร้าง skill+agent ใหม่ — `prototype-intake`/`prototype-writer`:** เพิ่ม `.claude/skills/prototype-intake/SKILL.md` และ `.claude/agents/prototype-writer.md` ตามที่ user ขอ — skill นี้จัดการทั้งกระบวนการสร้าง/ปรับปรุง UI/UX Prototype (อ้างอิง Requirement/Backlog/Feature List/User Journey ครบชุดหรือเฉพาะเจาะจงตามที่ user ระบุ), ตรวจสอบ `DESIGN.md` ก่อนเสมอ (ถ้ายังไม่มีต้องถาม user เพื่อสร้างก่อน — โทนสี/สไตล์/ภาพอ้างอิง พร้อมตัวเลือก ≥3 แนวทางเมื่อ user ไม่มีไอเดีย), เสนอแผนให้ user รีวิว/ยืนยันก่อนสร้างไฟล์จริงเสมอ, และเมื่อเรียกซ้ำกับ `{topic-slug}` ที่มี version เดิมอยู่แล้ว ต้องถามทุกครั้งว่าจะสร้าง version ใหม่หรือแก้ version ล่าสุด พร้อมคำแนะนำตามสถานการณ์จริง — Prototype เก็บที่ `.docs/02-design/01-prototypes/{topic-slug}/v{N}/` แยกจากไฟล์ `user-journey-{persona}.md` เดิมที่อยู่ระดับบนสุดของโฟลเดอร์เดียวกัน ไม่ทับกัน พบว่ามี draft skill/agent/DESIGN.md ที่คล้ายกันมากอยู่แล้วใน `.claude/worktrees/quirky-shtern-6ff1bb/` (worktree ที่เพิ่ง untrack ไปก่อนหน้านี้ในวันเดียวกัน) จากความพยายามครั้งก่อนที่ถูกทิ้งค้างไว้ — ใช้เป็นต้นแบบแต่ปรับให้ตรงกับสถานะจริงปัจจุบันของโปรเจกต์ (เช่น `feature-list.md`/`user-journey-{persona}.md` มีอยู่จริงแล้วไม่ต้อง derive เสมอไป, อ้างอิง Sprint ล่าสุดถึง 012/014 ไม่ใช่แค่ 007) — อัปเดต `CLAUDE.md` เพิ่ม `prototype-intake` เข้า Documentation pipeline table, เพิ่ม section "### Prototype generation", ปรับ bullet `01-prototypes/` ใน Repository structure, และเพิ่มหมายเหตุเรื่อง `DESIGN.md` ยังไม่มีอยู่จริงใน Working conventions — ถือโอกาสเดียวกันอัปเดต Project purpose ให้กล่าวถึง Version 3/Sprint 12 ที่เพิ่งสร้าง spec ไปด้วย (ยังเป็นแค่ spec ไม่ใช่ built) เพื่อไม่ให้ CLAUDE.md ล้าหลังทันทีหลังสร้าง spec
