# Log 20260830

- **Implement Sprint 12: Cloud Sync (Firebase Authentication + Firestore) — ตาม spec:** พัฒนาโค้ดครบตาม [[../01-requirements/01-spec/20260829-014-my-today-sprint12-cloud-sync|Sprint 12 spec]] — `npm install firebase` (+ `@firebase/rules-unit-testing` เป็น devDependency สำหรับทดสอบ Security Rules), สร้าง `src/lib/firebase.ts` (init app/auth/firestore), `src/hooks/useAuth.ts` (Google Sign-In ผ่าน `signInWithPopup` พร้อม fallback เป็น `signInWithRedirect` เมื่อ popup ถูกบล็อก — เจอจริงตอนทดสอบในเบราว์เซอร์สภาพแวดล้อมนี้), `src/lib/cloudSync.ts` (`pullAndMerge`/`pushDiff` แบบ generic ต่อ collection ด้วย timestamp-based last-write-wins ตาม Business Rule 8, บวก `pullProfile`/`pushProfile` สำหรับ Profile ซึ่งเป็น single-record ไม่ใช่ array), `src/hooks/useCloudSync.ts` (orchestrator: opt-in toggle เก็บที่ LocalStorage key แยกต่างหาก ไม่ผูกกับ Profile และไม่ sync ตัวมันเอง เพราะเป็นการตัดสินใจต่ออุปกรณ์ ไม่ใช่ข้อมูลต่อบัญชี — pull ครั้งเดียวตอน sync เพิ่งเปิด+login แล้ว push แบบ debounce 800ms ต่อ entity เมื่อข้อมูลเปลี่ยน), `firestore.rules` (per-user Security Rules: `request.auth.uid == uid` ต่อ path `users/{uid}/**`) — เพิ่ม `updatedAt: string` ให้ `Task`/`CalendarEvent`/`Note`/`Link`/`LifeArea`/`Profile` ทุกตัว (pure addition, normalize-on-read จาก `createdAt` สำหรับ record เก่า ไม่ bump storage key ตาม pattern `inInbox` ของ Sprint 8) — อัปเดต seed data (`seedTasks.ts`/`seedEvents.ts`/`seedLifeAreas.ts`) ให้มี `updatedAt` ครบ — เพิ่ม Cloud Sync card ในหน้า `/profile` (Sign-In/Sign-Out, toggle เปิด/ปิด sync, sync status indicator, error banner แบบ dismissible) — อัปเดต Privacy Notice (`/privacy`) ให้ครอบคลุมทั้ง 2 สถานะ sync ปิด/เปิดตาม scope
- **ทดสอบยืนยันในเบราว์เซอร์จริง:** non-regression เต็มรูปแบบ (สร้าง/แก้/ลบ Task ทำงานปกติทุกอย่างเหมือนก่อน Sprint 12, ไม่มี console error, `npm run build` ผ่าน — `tsc --noEmit` + vite build สำเร็จ), ยืนยัน `updatedAt` ตั้งค่าถูกต้องตอนสร้าง (เท่ากับ `createdAt`) และอัปเดตใหม่ตอนแก้ไข (ใหม่กว่า `createdAt`) — ทดสอบ Sign-In จริงพบ `auth/popup-blocked` (สภาพแวดล้อม sandbox บล็อก popup — ปัญหาจริงที่เกิดในเบราว์เซอร์บางแบบ ไม่ใช่แค่ sandbox นี้) ได้เพิ่ม fallback `signInWithRedirect` แล้วทดสอบซ้ำ พบว่าเรียกถึง Firebase backend จริงได้ แต่เจอ `auth/configuration-not-found` ต่อ — ทั้งสอง error แสดงผลถูกต้องเป็น dismissible banner ไม่ทำให้แอปพัง เป็นการยืนยันว่า error-handling path ทำงานถูกต้อง
- **พบ blocker จากภายนอก 2 จุด ทำให้ Gate 12 ยังไม่ผ่านครบ (ไม่ใช่บั๊กในโค้ด):** (1) Google Sign-In provider ยังไม่ได้เปิดใช้งานใน Firebase Console ของโปรเจกต์ `my-today-a25d9` จริง (`auth/configuration-not-found` ยืนยันเรื่องนี้ตรงๆ) — ต้องให้เจ้าของโปรเจกต์เข้า Firebase Console → Authentication → Sign-in method → เปิด Google เอง ไม่มีเครื่องมือให้ Claude Code ทำแทนได้ (2) พยายามรัน Security Rules ผ่าน Firebase Emulator ตามที่ Gate 12/Acceptance Criteria กำหนด — สร้าง [scripts/test-firestore-rules.mjs](../../scripts/test-firestore-rules.mjs) (ใช้ `@firebase/rules-unit-testing`, ครอบคลุม: user เข้าข้อมูลตัวเองได้, user อื่นเข้าไม่ได้, unauthenticated เข้าไม่ได้), เพิ่ม `firebase.json` + npm script `test:rules` แต่รันไม่ได้จริงเพราะ `firebase-tools` (v15) ต้องการ Java 21+ ในขณะที่เครื่องที่ใช้ build งานนี้มีแค่ Java 8 — ตรวจสอบ logic ของ `firestore.rules` ด้วยมือแล้วว่าถูกต้องตาม pattern มาตรฐานของ Firebase แต่ยังไม่ผ่านการรันทดสอบจริง
- **backlog.md / CLAUDE.md:** อัปเดต Sprint 12 entry ใน `backlog.md` เป็นสถานะ "กำลังดำเนินการ" (ไม่ใช่ "เสร็จแล้ว" เพราะ Gate 12 ยังไม่ผ่านครบ, ไม่ใช่ "ยังไม่เริ่ม" เพราะโค้ดสร้างและ non-regression ยืนยันแล้วจริง) พร้อมแจกแจงรายการที่ทำเสร็จ/ยังไม่ได้ทำอย่างละเอียด — อัปเดต CLAUDE.md: Project state (Sprint 12 code-complete แต่ยังไม่ verified), Project purpose (Version 3 bullet), Architecture (bullet ใหม่อธิบายไฟล์/hook ทั้งหมดของ Cloud Sync), Commands (`npm run test:rules` ใหม่ พร้อมหมายเหตุ Java 21+ requirement) — ถือโอกาสแก้ข้อความเก่าที่ผิดพลาดค้างมาก่อนหน้านี้ใน Project state ที่บอกว่า "Sprint 9-11 have not started yet" ทั้งที่จริง Sprint 9/10 เสร็จแล้วและ Sprint 11 ใกล้เสร็จ (สถานะจริงตาม `backlog.md`) — commit "b323d06 Implement Sprint 12: Cloud Sync (Firebase Auth + Firestore)" push ขึ้น `origin/master` แล้วตามที่ user ยืนยัน
- **Sprint 12 blocker แรกแก้ไขแล้ว (20260830 รอบถัดมา) — Google Sign-In provider เปิดใช้งานจริงแล้ว:** user เข้า Firebase Console เอง (พบว่า Authentication ไม่ได้อยู่ใต้เมนู "Build" ตรงๆ ใน UI เวอร์ชันปัจจุบัน ต้องหาผ่านช่องค้นหาหรือหมวด "Security") เปิด Enable ให้ Google ใน Authentication → Sign-in method สำเร็จ (ยืนยันจาก screenshot ที่ user ส่งมา 2 รูป — รูปแรกเผยว่ายังไม่ได้เปิด toggle, รูปตามมาหลัง user กด Enable+Save) — ทดสอบซ้ำในเบราว์เซอร์: กด "Sign in ด้วย Google" แล้ว `signInWithRedirect` ทำงานถูกต้อง redirect ไปถึง `accounts.google.com` จริงสำเร็จ (ยืนยันผ่าน `tabs_context` เห็น origin เปลี่ยนเป็น `https://accounts.google.com`) ต่างจากก่อนหน้านี้ที่ค้างที่ `auth/configuration-not-found` ก่อนจะหลุดจาก localhost ด้วยซ้ำ — ยืนยันว่า configuration ฝั่ง Firebase ถูกต้องสมบูรณ์แล้ว ไม่ได้กรอก credential จริงต่อ (Browser pane ของ Claude Code ไม่ใช่เบราว์เซอร์จริงของ user ไม่มี Google session ค้างอยู่ และการกรอก credential เป็นเรื่องที่ user ต้องทำเองเสมอ ตามที่ยืนยันชัดเจนกับ user ก่อน) — user เลือกหยุดตรงนี้ ถือว่าเพียงพอแล้ว — ทดสอบซ้ำ background console error (HTTP 400) ที่เคยพบด้วย พบว่า**ยังคงอยู่เหมือนเดิม** จึงตัดข้อสันนิษฐานเดิมที่ว่าเกี่ยวกับ Authentication provider ยังไม่เปิดออกไป ต้นตอที่แท้จริงยังไม่ทราบ แต่ยืนยันซ้ำว่าไม่กระทบการทำงานของแอป — อัปเดต `backlog.md` Sprint 12 entry ย้าย blocker แรกไปเป็น "แก้ไขแล้ว" เหลือ 2 อย่าง: Security Rules Emulator test (ต้องการ Java 21+) และการ login+multi-device sync แบบเต็มรูปแบบที่ user ต้องทำเองในเบราว์เซอร์จริง — ยังไม่ commit/push รอบนี้ รอ user ยืนยันตามธรรมเนียมเดิม
- **Requirement intake ใหม่ — สร้าง Sprint 13 spec: Smart Capture จากรูปภาพ (Version 3):** สร้าง [[../01-requirements/01-spec/20260830-015-my-today-sprint13-smart-capture-image|Sprint 13 spec]] (running no. 015 ต่อจาก 014) — เพิ่มตัวเลือก "สแกนจากรูปภาพ" ใน Quick Capture เฉพาะประเภท Event เท่านั้น ให้ AI vision วิเคราะห์ภาพสกัดชื่องาน/วันที่/เวลา/สถานที่มาเติมฟอร์มให้ผู้ใช้ตรวจสอบ/แก้ไขก่อนบันทึกเสมอ (ไม่ auto-submit) — ต้อง sign in ด้วย Google ก่อนใช้ (feature แรกของแอปที่ gate ด้วย sign in, ระบุชัดว่าเป็น exception เฉพาะจุด ไม่กระทบ positioning "ไม่ต้องมีบัญชี" ของ Quick Capture ประเภทอื่น) — ใช้ Vercel Serverless Function ใหม่เป็น proxy เก็บ AI API key ฝั่ง server เท่านั้น (exception ข้อที่ 2 ต่อกฎ "ไม่มี backend" แยกจาก Firebase exception ของ Sprint 12) — ระบุชัดเจนในเอกสารว่าไม่ใช่และไม่นับเป็นจุดเริ่มต้นของ "Daily Orchestrator" ที่ Project purpose จองชื่อไว้แยกต่างหากเป็น Phase หลัง Freeze — รูปภาพประมวลผลแบบ ephemeral ไม่เก็บถาวรที่ไหน — อัปเดต `backlog.md` เพิ่มแถวใหม่สถานะ "ยังไม่เริ่ม" พร้อม wikilink กลับไปสเปกนี้
- **feature-journey-sync — อัปเดต feature-list.md + user-journey-*.md (targeted):** เพิ่มแถวที่ 12-13 ใน [[../01-requirements/feature-list|feature-list.md]] (ตารางสรุป + subsection รายละเอียด) — Sprint 12: Cloud Sync (MoSCoW **Should**, สถานะ "กำลังดำเนินการ") และ Sprint 13: Smart Capture จากรูปภาพ (MoSCoW **Could**, สถานะ "ยังไม่เริ่ม") ทั้งสองแถวระบุคอลัมน์ FR ว่า "ยังไม่มี FR-ID กำหนด" พร้อมหมายเหตุว่า Functional Requirements Master List (008) ยังไม่อัปเดตครอบคลุม Sprint 12/13 — รีเฟรช subsection ของ Sprint 11 แบบ targeted (ต่อท้ายคำอธิบายเดิม ไม่เปลี่ยน MoSCoW) สะท้อนความคืบหน้า Black Box Testing (20260828) และ UX/UI re-verify (20260829) จาก backlog.md — เพิ่ม Change Log entry ใหม่ในเอกสาร — ตรวจสอบ [[../02-design/01-prototypes/user-journey-student|user-journey-student.md]] และ [[../02-design/01-prototypes/user-journey-general-person|user-journey-general-person.md]] แล้วพบว่า core diagram/steps ทั้งสองไฟล์ยังถูกต้องครบถ้วน ไม่ต้องเพิ่ม step ใหม่ (Sprint 12/13 เป็น optional add-on นอก core Final Journey ของทั้งสอง persona) แก้ไขแบบ targeted เฉพาะย่อหน้า "หมายเหตุสถานะ" ในทั้งสองไฟล์ให้ไม่ระบุว่า Sprint 11 "ยังไม่เริ่ม" อีกต่อไป (ล้าสมัย — จริงๆ คือ "กำลังดำเนินการ" ตาม backlog.md ปัจจุบัน) พร้อมอธิบายว่าเหตุใด Sprint 12/13 จึงไม่กระทบไดอะแกรม
- **architecture-doc-sync — อัปเดต [[../02-design/02-technical/architecture|architecture.md]] สะท้อน Sprint 12 (build แล้ว) + Sprint 13 (มีแค่ spec):** หัวข้อ 1 (System Context) — แก้ประโยค "ไม่มี external system dependency ใดๆ" ให้มีเงื่อนไข พร้อมเพิ่ม External Identity Provider + Cloud Data Store เป็นกล่อง optional/opt-in (เส้นประ) ใน Context diagram; หัวข้อ 2 (Container View) — เพิ่ม container ใหม่ "Cloud Synchronization Service" (เชื่อมผ่าน Application/Domain Logic Layer เพื่อรักษาหลักการจุดเดียวเป็นเจ้าของ state) พร้อมหมายเหตุการ implement ปัจจุบันที่แม่นยำต่อ container (Firebase Authentication + Cloud Firestore, ไฟล์จริงที่เกี่ยวข้อง) — ดึงจาก spec/backlog.md โดยตรงเนื่องจาก `tech-stack.md` ยังไม่ครอบคลุม Sprint 12; หัวข้อ 3 — เพิ่มประโยคอธิบายแนวคิด "sync-eligible" (timestamp last-write-wins); หัวข้อ 4 — ไม่แก้ไข (ยืนยันแล้วว่า Sprint 12/13 ไม่อยู่ใน core journey); หัวข้อ 5 — แก้ 3 bullet (no-backend/local-first, privacy-by-architecture, AI/external-integration) ให้มีเงื่อนไขสะท้อน Cloud Sync เป็นข้อยกเว้นแรก; หัวข้อ 6 — เพิ่ม known gap ใหม่ 5 bullet สำหรับ Sprint 13 (Server-side Integration Proxy, External AI Analysis Service, authentication-gated ครั้งแรกของระบบ, ย้ำว่าไม่ใช่ Daily Orchestrator) และตัด Sprint 12 ออกจากหัวข้อนี้ (โปรโมทเข้า diagram หลักแล้ว); เพิ่ม Change Log entry ใหม่ในเอกสาร — `tech-stack.md` ไม่ได้ถูกแก้ไข ใช้เป็น source ของหมายเหตุ implement เดิม (Sprint 1-8) และดึงรายละเอียด Sprint 12 จาก spec/backlog.md โดยตรงแทน เนื่องจาก `tech-stack.md` ยังไม่ถูกอัปเดตให้ครอบคลุม Sprint 12
- **data-api-doc-sync — อัปเดต [[../02-design/02-technical/database-schema|database-schema.md]] + [[../02-design/02-technical/api-spec|api-spec.md]] สะท้อน Sprint 12 (build แล้ว) + Sprint 13 (มีแค่ spec):** อ่าน [[../02-design/02-technical/architecture|architecture.md]] ที่เพิ่งอัปเดตวันเดียวกัน + spec Sprint 12/13 ก่อนเขียน ไม่พบ conflict ใดๆ ต่อ container/ความสัมพันธ์ที่ระบุไว้ **database-schema.md:** เพิ่มฟิลด์ `updatedAt: DateTime` (required) ให้ LIFE_AREA/TASK/EVENT/NOTE/LINK/PROFILE ทั้ง ER diagram (หัวข้อ 1) และตารางฟิลด์ (2.1-2.3, 2.5-2.7) — ไม่ใส่ FILE (นอกขอบเขต sync ของ Sprint 12) และไม่ใส่ NOTIFICATION_READ_STATE (ไม่ sync); เพิ่ม cross-cutting field ใหม่ในหัวข้อ 3 อธิบาย `updatedAt` เป็นฐาน last-write-wins; เพิ่มบรรทัดยืนยันในหัวข้อ 4 ว่า Sprint 13 ไม่ต้องการฟิลด์/entity ใหม่เลย; เพิ่มย่อหน้าใหม่ในหมายเหตุ "การ implement ปัจจุบัน" อธิบาย Cloud Sync mirroring ไปยัง Cloud Firestore (ตาม path `users/{uid}/{collection}/{id}`) เป็นส่วนเสริมนอกเหนือจาก LocalStorage ไม่ใช่แทนที่ **api-spec.md:** เพิ่ม section ใหม่ "3. Cloud Synchronization Operations" (Sign In, Sign Out, Enable/Disable Cloud Sync, Pull and Merge from Cloud, Push Diff to Cloud) แทรกก่อน Derived/Read-only Operations เดิม แล้ว renumber หัวข้อถัดไปทั้งหมด (Derived Operations 3→4, Known Gaps 4→5, Change Log 5→6) พร้อมแก้ cross-reference ภายในเอกสาร; เพิ่ม bullet ใหม่ในหัวข้อ 5 Known Gaps สำหรับ "Analyze Image for Event Capture" (Sprint 13) ระบุว่าเป็นข้อยกเว้นที่ 2 ต่อหลักการ local-function-call แยกจาก Cloud Sync โดยสิ้นเชิง; เพิ่มย่อหน้าใหม่ในหมายเหตุ implement ปัจจุบันอธิบายว่า Cloud Sync operations implement จริงที่ `src/hooks/useAuth.ts`/`src/lib/cloudSync.ts`/`src/hooks/useCloudSync.ts` และเป็น operation กลุ่มแรกที่ทำ network call จริง — ทั้งสองเอกสารเพิ่ม Change Log entry ใหม่ — ไม่พบ backlog.md ล้าสมัย (ตรวจสอบผ่าน `backlog-sync-check` มาแล้วในรอบนี้)
- **detailed-design-doc-sync — อัปเดต [[../02-design/02-technical/detailed-design|detailed-design.md]] สะท้อน Sprint 12 (build แล้ว, ได้ diagram จริง) + Sprint 13 (มีแค่ spec, known gap เท่านั้น) — ขั้นตอนสุดท้ายของ doc-sync chain รอบนี้ (backlog-sync-check → feature-journey-sync → architecture-doc-sync → data-api-doc-sync → detailed-design-doc-sync):** อ่าน architecture.md/api-spec.md/database-schema.md ที่เพิ่งอัปเดตวันเดียวกันก่อนเขียน ไม่พบ conflict ใดๆ — ยืนยันจาก feature-journey-sync ล่าสุดว่า Sprint 12/13 ไม่อยู่ใน core persona journey ทั้งสอง จึง**ไม่แก้ไข §1 เลย** **§2:** เพิ่ม **2.7 Enable Cloud Sync** เป็น sequence diagram เต็มรูปแบบใหม่ (participant ใหม่ `CloudSync`/`IdentityProvider`/`CloudStore` ดึงจาก container/external system ของ architecture.md เท่านั้น ไม่เพิ่ม container ใหม่) แสดง Sign In → Enable Cloud Sync (pull+merge last-write-wins ด้วย `updatedAt`) พร้อมย่อหน้าอธิบาย Push Diff to Cloud ต่อท้ายแบบเดียวกับ 2.6 — เป็นไดอะแกรมแรกในเอกสารทั้งฉบับที่มี network call จริง **§3:** เพิ่มโน้ต error/edge-case สำหรับ 2.7 **§4:** ตัด Sprint 12 ออก (โปรโมทเข้า diagram แล้ว) เพิ่ม known gap ใหม่ "Analyze Image for Event Capture" (Sprint 13, prose เท่านั้น ไม่มี diagram) **§5:** เพิ่ม subsection "2.7 Enable Cloud Sync" อ้างอิงไฟล์จริง (`ProfilePage.tsx`, `useCloudSync.ts`, `cloudSync.ts`, `useAuth.ts`, Firebase Auth/Firestore) ดึงจาก spec/backlog.md เนื่องจาก `tech-stack.md` ยังไม่ครอบคลุม Sprint 12 (แนะนำรัน `tech-stack-advisor` อีกรอบ) และแก้หัวข้อปิดท้ายเดิม "เหตุผลที่ทุก message เป็น local call" ให้มีเงื่อนไข (ยกเว้น §2.7) — เพิ่ม Change Log entry ใหม่ในเอกสาร ไม่พบ conflict ใดๆ กับ architecture.md/api-spec.md/database-schema.md
