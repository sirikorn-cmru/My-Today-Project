# Backlog

รายการ Backlog ที่รวบรวมมาจากเอกสารใน [[01-spec/index|01-spec]] แต่ละรายการลิงก์กลับไปยังเอกสาร spec ต้นทาง

- [x] ~~My Today — Web Application จัดการภารกิจประจำวันสำหรับนักศึกษา (Sprint 1: MVP)~~ — [[../00-archived/20260806-001-my-today-sprint1-mvp|001, archived]] — สถานะ: ถูกแทนที่ด้วย roadmap Sprint 1-6 ใหม่ (ดูรายการด้านล่าง)
- [x] My Today — Sprint 1: Today Dashboard — [[01-spec/20260806-001-my-today-sprint1-today-dashboard|001]] — สถานะ: เสร็จแล้ว (หมายเหตุ 20260806: generalize target user จาก "นักศึกษาเท่านั้น" เป็น "บุคคลทั่วไป โดยยังรองรับนักศึกษา" แล้ว — field "รายวิชา" ถูกแทนที่ด้วย "Life Area") (ตรวจสอบ 20260806: พบว่า build แล้วจริงจาก commit "371a396 Implement Sprint 1: Today Dashboard (mock data)" — src/pages/DashboardPage.tsx และ components ครบตาม scope, อัปเดตสถานะ)
- [x] My Today — Sprint 2: Task Management — [[01-spec/20260806-002-my-today-sprint2-task-management|002]] — สถานะ: เสร็จแล้ว (หมายเหตุ 20260806: generalize target user จาก "นักศึกษาเท่านั้น" เป็น "บุคคลทั่วไป โดยยังรองรับนักศึกษา" แล้ว — field "รายวิชา" ถูกแทนที่ด้วย "Life Area") (ตรวจสอบ 20260806: พบว่า build แล้วจริงจาก commit "647f490 Implement Sprint 2: Task Management" — src/hooks/useTasks.ts (CRUD + LocalStorage) และ src/pages/TasksPage.tsx (filter/search/sort) ครบตาม scope; หมายเหตุ: field ยังเป็น `Task.subject` ไม่ใช่ `Task.lifeAreaId` เนื่องจาก Sprint 7 (retrofit Life Area) ยังไม่เริ่ม ซึ่งตรงตามลำดับ roadmap, อัปเดตสถานะ)
- [x] My Today — Sprint 3: Calendar & Schedule — [[01-spec/20260806-003-my-today-sprint3-calendar-schedule|003]] — สถานะ: เสร็จแล้ว (หมายเหตุ 20260806: generalize target user จาก "นักศึกษาเท่านั้น" เป็น "บุคคลทั่วไป โดยยังรองรับนักศึกษา" แล้ว — เพิ่มการเชื่อมโยง Life Area กับกิจกรรม/นัดหมาย/ตารางเรียน) (ตรวจสอบ 20260807: พบว่า build แล้วจริงจาก commit "1f89ea3 Implement Sprint 3: Calendar & Schedule" — src/pages/CalendarPage.tsx (Today/Week/Month), src/components/EventFormModal.tsx, src/components/DayAgenda.tsx, src/lib/calendarUtils.ts, src/hooks/useEvents.ts ครบตาม scope, อัปเดตสถานะ)
- [x] My Today — Sprint 4: File Organizer — [[01-spec/20260806-004-my-today-sprint4-file-organizer|004]] — สถานะ: เสร็จแล้ว (หมายเหตุ 20260806: generalize target user จาก "นักศึกษาเท่านั้น" เป็น "บุคคลทั่วไป โดยยังรองรับนักศึกษา" แล้ว — field "รายวิชา" ถูกแทนที่ด้วย "Life Area") (ตรวจสอบ 20260807: พบว่า build แล้วจริงจาก commit "e71bd97 Implement Sprint 4: File Organizer" — src/pages/FilesPage.tsx, src/components/FileFormModal.tsx / FileCard.tsx, src/lib/fileDb.ts (IndexedDB), src/hooks/useFiles.ts, การเชื่อมไฟล์กับ Task ครบตาม scope, อัปเดตสถานะ)
- [x] My Today — Sprint 5: Notification & Deadline Awareness — [[01-spec/20260806-005-my-today-sprint5-notification-deadline-awareness|005]] — สถานะ: เสร็จแล้ว (ตรวจสอบ 20260807: พบว่า build แล้วจริงจาก commit "91a49b5 Implement Sprint 5: Notification & Deadline Awareness" — src/pages/NotificationsPage.tsx, src/components/NotificationBell.tsx / NotificationList.tsx, src/lib/notificationUtils.ts (Due Today/Due Soon/Overdue), src/hooks/useNotifications.ts ครบตาม scope, อัปเดตสถานะ)
- [x] My Today — Sprint 6: Integration, UX & Final Testing (Final Sprint ของ Version 1 / Core) — [[01-spec/20260806-006-my-today-sprint6-integration-ux-final-testing|006]] — สถานะ: เสร็จแล้ว (หมายเหตุ 20260806: เพิ่มข้อกำหนดด้านกฎหมาย IT และ PDPA เข้าไปในเอกสารแล้ว; เพิ่ม dual-persona acceptance criteria (นักศึกษา + บุคคลทั่วไป) สำหรับ generalize target user แล้ว; ปิดจบ Version 1/Core ที่นี่ — Sprint 7-12 คือ Competition Track/Version 2 ต่อยอด) (ตรวจสอบ 20260807: เพิ่ม Footer + PrivacyPage (Privacy Notice/Terms of Use เข้าถึงได้ทุกหน้า), vercel.json (SPA rewrite เตรียม deploy บน Vercel), error handling ใน useFiles; ทดสอบ Final User Journey ทั้งสอง persona (นักศึกษา + บุคคลทั่วไป) end-to-end ผ่าน Core เดียวกันจริง, responsive ที่ Mobile/Tablet/Desktop ไม่ overflow, ยืนยันไม่มี network request ออกนอกเครื่อง — Version 1/Core เสร็จสมบูรณ์)
- [x] My Today — Sprint 7: Life Area / Workspace & Personal Profile (Competition Track เริ่มที่นี่) — [[01-spec/20260806-007-my-today-sprint7-category-profile|007]] — สถานะ: เสร็จแล้ว (เดิมชื่อ Sprint 2.5 ก่อน renumber เป็น Sprint 7 ตาม roadmap "One Life, One Workspace"; retrofit Life Area/Profile เข้า Sprint 1-2 ที่ build แล้ว และเป็น prerequisite ของ Sprint 3-6, 8-12) (ตรวจสอบ 20260808: พบหลักฐาน build ครบตาม scope ใน working tree — src/hooks/useLifeAreas.ts + src/pages/LifeAreasPage.tsx (CRUD Life Area), src/hooks/useProfile.ts + src/pages/ProfilePage.tsx (CRUD Personal Profile), src/types.ts มี `LifeArea`/`LifeAreaInput` และ `Task.lifeAreaId`/`CalendarEvent.lifeAreaId`/`FileRecord.lifeAreaId` แทนที่ `Task.subject` เดิมแล้ว (breaking change ตาม business rule ข้อ 3), เชื่อมกับ Task/Event/File และมี route/nav link `/life-areas`, `/profile` ใน App.tsx และ Header.tsx ครบ — แต่ยังไม่มี commit "Implement Sprint 7" ขึ้น git log (การเปลี่ยนแปลงยังอยู่ใน working tree ยังไม่ commit ณ วันที่ตรวจสอบ), อัปเดตสถานะ) (ตรวจสอบ 20260816: พบ commit "80acbf1 Implement Sprint 7: Life Area & Personal Profile" ขึ้น git log แล้ว — commit ตามมาหลังจากการตรวจสอบครั้งก่อน จึงยืนยันสถานะเสร็จแล้วสมบูรณ์ทั้งโค้ดและ commit)
- [ ] My Today — Functional Requirements Master List (เอกสารอ้างอิงกลาง ไม่ใช่ Sprint) — [[01-spec/20260806-008-my-today-functional-requirements-master|008]] — สถานะ: ยังไม่เริ่ม (รวม FR ทั้งหมดของระบบพร้อม cross-link ไปยัง Sprint spec ที่เกี่ยวข้อง; หมายเหตุ 20260806: เพิ่ม FR-13 ถึง FR-19 ครอบคลุม Competition Track Sprint 8-10 แล้ว)
- [x] My Today — Sprint 8: Universal Inbox + Quick Capture (Competition Track) — [[01-spec/20260806-009-my-today-sprint8-universal-inbox-quick-capture|009]] — สถานะ: เสร็จแล้ว (Quick Capture กลาง "+ Add to My Today" + หน้า My Inbox + จัดเข้า Life Area ภายหลัง + entity ใหม่ Note/Link; ไม่มี AI parsing ข้อความอิสระใน Sprint นี้) (ตรวจสอบ 20260823: พบว่า build แล้วจริงจาก commit "508e98d Implement Sprint 8: Universal Inbox + Quick Capture" — src/components/QuickCaptureModal.tsx, src/pages/InboxPage.tsx, src/hooks/useNotes.ts / useLinks.ts, src/components/NoteFormModal.tsx / LinkFormModal.tsx, src/types.ts มี `Note`/`Link`/`InboxKind` และ `inInbox` บน Task/CalendarEvent/FileRecord/Note/Link ครบตาม scope, wiring ครบใน App.tsx, อัปเดตสถานะ)
- [x] My Today — Sprint 9: Now/Next/Later Timeline + Smart Priority + Life Progress (Competition Track) — [[01-spec/20260806-010-my-today-sprint9-timeline-priority-progress|010]] — สถานะ: เสร็จแล้ว (รวม Timeline Now/Next/Later, Smart Priority sorting แบบกฎตายตัว, และ Life Progress แยกตาม Life Area เข้าเป็น Sprint เดียว; ไม่มี AI/gamification/social comparison ใน Sprint นี้) (ตรวจสอบ 20260824: พบหลักฐาน build ครบตาม scope ใน working tree — src/lib/timelineUtils.ts (ใหม่) มี `smartPriorityTier`/`sortTasksBySmartPriority` (Business Rule 2: Overdue→Due Today→Upcoming→High Priority→Normal) และ `getTimelineEntries` (Business Rule 1: Now/Next/Later เฉพาะวันนี้) และ `getLifeProgress` (Business Rule 3: Done vs รวมที่ครบกำหนดวันนี้ แยกตาม Life Area ไม่มีคำว่า "Score" เลย), src/components/LifeProgress.tsx + src/components/TimelineSection.tsx (ใหม่), src/pages/TimelinePage.tsx (ใหม่, route `/timeline` เพิ่มใน App.tsx), src/components/NavBar.tsx เพิ่มแท็บ "Timeline", src/pages/DashboardPage.tsx เรียง Today's Tasks ด้วย `sortTasksBySmartPriority` และแสดง `<LifeProgress>` ครบตาม scope, อัปเดตสถานะ) (ตรวจสอบ 20260824: พบ commit "e453ba7 Implement Sprint 9: Now/Next/Later Timeline + Smart Priority + Life Progress" (แก้เฉพาะ src/) และ "eebf267 Sync requirements/design docs for Sprint 9 completion" (แก้เฉพาะ .docs/) ขึ้น git log แล้ว — commit ตามมาหลังจากการตรวจสอบครั้งก่อน จึงยืนยันสถานะเสร็จแล้วสมบูรณ์ทั้งโค้ดและ commit, แก้ไขหมายเหตุเดิมที่ระบุว่ายังไม่ commit)
- [x] My Today — Sprint 10: Task–Event–File Linking (What + When + Information) (Competition Track) — [[01-spec/20260806-011-my-today-sprint10-task-event-file-linking|011]] — สถานะ: เสร็จแล้ว (เชื่อม Task/Event กับ Note/Link เพิ่มจาก File เดิมของ Sprint 4, หน้า Task/Event Detail แบบ What/When/Information ในหน้าเดียว, custom reminder lead time override default ของ Sprint 5) (แก้ไขสเปก 20260823: Business Rule ข้อ 1 เดิมอ้างถึง `Task.linkedFileIds` ซึ่งไม่มีอยู่จริง — แก้เป็นระบุว่า Task↔File จาก Sprint 4 เก็บที่ฝั่ง File ผ่าน `linkedTaskIds` ไม่ใช่ field บน Task) (ตรวจสอบ 20260824: พบหลักฐาน build ครบตาม scope ใน working tree — src/types.ts มี `Task.linkedNoteIds`/`linkedLinkIds`/`reminderLeadTime` และ `CalendarEvent` เช่นเดียวกัน, `FileRecord.linkedEventIds` ใหม่ (Business Rule 4); src/components/TaskDetailModal.tsx และ EventDetailModal.tsx (ใหม่ทั้งคู่) จัดหน้าเป็น What/When/Information ตาม Business Rule 3 ครบ พร้อม reminder lead time selector (src/lib/taskUtils.ts มี `reminderLeadTimeOptions`/`formatLeadTime`); src/lib/notificationUtils.ts's `taskLevel`/`eventLevel` override threshold default ของ Sprint 5 ด้วย `reminderLeadTime` เมื่อตั้งไว้ตาม Business Rule 2; src/hooks/useFiles.ts เพิ่ม `linkFileToEvent`/`unlinkFileFromEvent`; src/hooks/useTasks.ts, useEvents.ts, useFiles.ts มี normalize-on-read (`?? []`/`?? null`) กัน record เดิมพัง; src/components/TaskCard.tsx/DayAgenda.tsx มีปุ่ม "รายละเอียด" เปิด Detail modal ครบ, wiring ผ่าน src/App.tsx ครบ (notes/links/linkFileToEvent/unlinkFileFromEvent); src/components/TaskFormModal.tsx แก้ไขให้คง `linkedNoteIds`/`linkedLinkIds`/`reminderLeadTime` เดิมไว้ตอนแก้ไขผ่านฟอร์มปกติ ไม่ถูกล้าง; seed data (seedTasks/seedEvents/seedFiles) อัปเดต field ใหม่ครบ — ยืนยันแล้วจาก commit "b03f83c Implement Sprint 10: Task-Event-File Linking (What+When+Information)")
- [ ] My Today — Sprint 11: Competition Demo + User Journey + UX Polish, then Freeze (Final Sprint ของ Competition Track / Version 2) — [[01-spec/20260806-012-my-today-sprint11-competition-demo-freeze|012]] — สถานะ: กำลังดำเนินการ (ไม่เพิ่ม Feature ใหม่ เน้น Integration/UX polish/Black Box Testing/Cleanup ของ Sprint 7-10 พร้อมเตรียม Competition Demo script คู่ persona + Positioning Narrative แล้ว Freeze ปิดรับ Feature ใหม่เข้า Version 2) (เพิ่มเติม 20260823: ผนวก 3 หัวข้อย่อย NFR จาก [[01-spec/20260823-013-my-today-non-functional-requirements-master|NFR Master List]] เข้า scope ของ Sprint นี้แล้ว — Accessibility Baseline (NFR-04) และ Browser Compatibility Matrix (NFR-06) ขยายให้ครอบคลุมทั้งระบบ Sprint 1-10 ไม่ใช่แค่ Sprint 7-10, และ IndexedDB Quota-Warning (NFR-08) เพิ่มเป็นงานใหม่ที่ได้รับยกเว้นจาก Business Rule ห้ามเพิ่ม Feature ใหม่ (pattern เดียวกับ Privacy Notice ของ Sprint 6)) (ตรวจสอบ 20260825: สถานะเดิม "ยังไม่เริ่ม" ไม่ตรงกับความจริงอีกต่อไป — พบหลักฐาน build บางส่วนใน working tree (ขณะตรวจสอบยังไม่ commit ขึ้น git log — ไม่มี commit "Implement Sprint 11" ใดๆ) จึงปรับเป็นสถานะกลาง "กำลังดำเนินการ" (สามสถานะ ต่างจากที่โปรเจกต์นี้เคยใช้แค่ "เสร็จแล้ว"/"ยังไม่เริ่ม" มาก่อน เพราะ "เสร็จแล้ว" จะ overclaim ในเมื่อ Gate 11 ยังไม่ได้ทดสอบ และ "ยังไม่เริ่ม" จะซ่อนความคืบหน้าจริงที่ตรวจสอบแล้ว) (ตรวจสอบ 20260825 รอบถัดมา: งานที่กล่าวถึงได้ commit ขึ้น git log แล้วเป็น "2f63c65 Apply Sprint 11: IndexedDB Quota-Warning + Accessibility <main> landmark" — สถานะ "กำลังดำเนินการ" ยังคงเดิม เนื่องจากเป็นเพียงส่วนหนึ่งของ Sprint 11 เท่านั้น ดูรายละเอียดที่หมายเหตุสำคัญด้านล่าง)

  **สิ่งที่ทำเสร็จแล้ว (ตรวจสอบในโค้ดจริง 20260825):**
  - IndexedDB Quota-Warning (NFR-08) — เสร็จ: `src/lib/storageQuota.ts` (ใหม่ — `getStorageEstimate` ครอบ `navigator.storage.estimate()` แบบ progressive enhancement, `QUOTA_WARNING_THRESHOLD = 0.8`, `formatQuotaWarning`), `src/hooks/useFiles.ts` เพิ่ม `quotaWarning`/`clearQuotaWarning` (เช็คตอนโหลดและหลัง `addFile` ทุกครั้ง), `src/pages/FilesPage.tsx` แสดง banner เตือนสีอำพันแบบ dismiss ได้ — ทดสอบจริงในเบราว์เซอร์โดย mock `navigator.storage.estimate()` เป็น 85% แล้ว banner ขึ้นถูกต้อง dismiss ได้ ไม่มี console error
  - Accessibility Baseline (NFR-04) เฉพาะส่วน semantic HTML — เสร็จ: ตรวจทั้ง `src/` แล้วไม่พบ `<div onClick>`, ปุ่ม icon-only มี `aria-label` ครบ, emoji ตกแต่งมี `aria-hidden` ครบ, input มี `<label htmlFor>`/`id` ครบ, focus ring ผ่าน `uiClasses.ts` ครบอยู่แล้ว (คาดว่ามาจากงาน UI/UX polish รอบก่อนหน้า) — ช่องโหว่ที่พบและแก้แล้ว: ทั้ง 10 หน้า (`DashboardPage`, `TasksPage`, `CalendarPage`, `TimelinePage`, `FilesPage`, `InboxPage`, `NotificationsPage`, `PrivacyPage`, `LifeAreasPage`, `ProfilePage`) ไม่เคยมี `<main>` landmark — เปลี่ยนเป็น `<main>` ครบทั้ง 10 หน้าแล้ว ตรวจสอบว่าแต่ละ route render `<main>` เดียวไม่ซ้ำ ไม่มี layout regression
  - Cleanup mock/debug code — เสร็จ: grep ทั้ง `src/` หา `console.log/debug/warn`, `TODO`, `FIXME`, `debugger`, `mock`/`Mock` ไม่พบเลยสักจุด ไม่มีอะไรต้องลบ
  - Integration check — เสร็จแบบเบา ๆ: เดินผ่าน Dashboard/Files/Tasks/Calendar/Timeline ในเบราว์เซอร์ ไม่มี console error ไม่มี regression จากการเปลี่ยน `<main>` และฟีเจอร์ Quota-Warning ใหม่

  **สิ่งที่ยังไม่ได้ทำ (ยังต้องทำก่อนจะถือว่า Sprint 11 "เสร็จแล้ว"):**
  - Browser Compatibility Matrix (NFR-06) — ยังไม่ได้ทำ (ยังไม่มีการแก้โค้ด และ environment นี้มีแค่ browser preview ที่เป็น Chromium ตัวเดียว ทดสอบข้าม browser จริง เช่น Firefox/Safari ไม่ได้ในนี้)
  - Competition Demo script (Business Rule ข้อ 3, Positioning Narrative) — ยังไม่ได้เขียน
  - Black Box Testing แบบเป็นทางการครบทุก FR ของ Sprint 7-10 พร้อมเอกสารผลทดสอบใน `.docs/03-testing/02-test-result/` — ยังไม่ได้ทำ มีแค่ smoke-check integration เบา ๆ ไม่ใช่การทดสอบทีละ FR อย่างเป็นระบบ
  - การตรวจสอบ UX/UI มาตรฐานเดียวกับ Sprint 6 แบบครบถ้วน (Empty State/Loading State/Validation/Confirmation ก่อน Delete/Error Message) ทีละหน้าจอ Sprint 7-10 — ยังไม่ได้ re-verify เป็นระบบในรอบนี้ (มีงาน "UI/UX polish ทั้งแอป" รอบก่อนหน้าที่อาจครอบคลุมส่วนใหญ่ไว้แล้ว แต่รอบนี้ไม่ได้ตรวจยืนยันใหม่)
  - Gate 11 (Final Competition User Journey end-to-end ทั้งสอง persona + Demo script พร้อม) — ยังไม่ได้ลอง

  **หมายเหตุสำคัญ:** งานข้างต้น commit ขึ้น git log แล้ว (ตรวจสอบ 20260825): commit "2f63c65 Apply Sprint 11: IndexedDB Quota-Warning + Accessibility <main> landmark" แก้ `src/App.tsx`, `src/hooks/useFiles.ts`, `src/lib/storageQuota.ts` (ใหม่), และทั้ง 10 หน้า `src/pages/*.tsx` (เพิ่ม `<main>` landmark) ตรงกับรายการไฟล์ที่ระบุไว้ก่อนหน้าครบถ้วน; commit "c45a9d6 Sync requirements/design docs for partial Sprint 11 progress" แก้เฉพาะ `.docs/` (backlog.md/feature-list.md/architecture.md/database-schema.md/api-spec.md/detailed-design.md) ตามหลัง — สถานะยังคง "กำลังดำเนินการ" เหมือนเดิม ไม่เปลี่ยนเป็น "เสร็จแล้ว" เนื่องจาก Browser Compatibility Matrix, Competition Demo script, formal Black Box Testing, และ Gate 11 ยังไม่ได้ทำ)
- [ ] My Today — Non-Functional Requirements Master List (เอกสารอ้างอิงกลาง ไม่ใช่ Sprint) — [[01-spec/20260823-013-my-today-non-functional-requirements-master|013]] — สถานะ: ยังไม่เริ่ม (รวม NFR ทั้งระบบ 9 หมวด สังเคราะห์ย้อนหลังจาก Sprint 1/4/5/6/7/8/9/11, `tech-stack.md`, `architecture.md` — 6 หมวด Performance/Reliability/Usability/Security/Offline/Maintainability สอดคล้องกับสิ่งที่ Sprint ที่เกี่ยวข้อง implement ไปแล้ว ไม่ต้องพัฒนาเพิ่ม; แต่ 3 หัวข้อย่อยใหม่ที่เพิ่งปิดเป็นข้อกำหนดครั้งแรกในเอกสารนี้ — Accessibility Baseline (NFR-04), Browser Compatibility Matrix (NFR-06), IndexedDB Quota-Warning (NFR-08) — ยังไม่ผูกกับ Sprint ใดและยังไม่เริ่มพัฒนา แนะนำให้พิจารณารวมเข้า Sprint 6/11 หรือ Sprint ใหม่ในอนาคตตอนลงมือพัฒนาจริง) (เพิ่มเติม 20260823: NFR-04/06/08 ถูกผูกเข้ากับ [[01-spec/20260806-012-my-today-sprint11-competition-demo-freeze|Sprint 11]] อย่างเป็นทางการแล้ว ไม่ใช่ "ไม่ผูกกับ Sprint ใด" อีกต่อไป — ดูรายละเอียดใน section "เพิ่มเติม (20260823)" ของสเปก Sprint 11) (ตรวจสอบ 20260825: อัปเดตสถานะการพัฒนาของทั้ง 3 หัวข้อย่อยตาม Sprint 11 ที่ตอนนี้ "กำลังดำเนินการ" — NFR-04 Accessibility Baseline (เฉพาะส่วน semantic HTML) และ NFR-08 IndexedDB Quota-Warning **เสร็จแล้วในโค้ดจริงและ commit ขึ้น git log แล้ว** (commit "2f63c65 Apply Sprint 11: IndexedDB Quota-Warning + Accessibility <main> landmark"); NFR-06 Browser Compatibility Matrix **ยังไม่ได้ทำ** ดูรายละเอียดหลักฐานทั้งหมดที่รายการ Sprint 11 ด้านบน)
