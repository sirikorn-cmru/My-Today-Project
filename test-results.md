# test-results.md — ผลการรันเทสต์ทั้งหมดของโปรเจกต์ My Today

เอกสารนี้สรุปผลการรัน **ทุกชุดทดสอบที่โปรเจกต์นี้มี** ในรอบวันที่ **2026-09-21** ไว้ในที่เดียว — ทั้งชุดที่รันด้วยคำสั่ง (lint / type-check+build / Firestore Security Rules) และชุดที่รันด้วยการคลิกจริงในเบราว์เซอร์ (Full App Smoke Test 37 เคส)

- **สภาพแวดล้อม:** Windows 10 Pro, Node + npm, dev server ผ่าน `npm run dev` ที่ `http://localhost:5173`
- **Branch/Commit ที่ทดสอบ:** `master` (ณ commit `2993ff9` + การเพิ่ม ESLint config ในรอบนี้)
- **เวลาทั้งหมดเป็นเวลาไทย (UTC+7)**
- รายละเอียดเชิงลึกรายเคสของ Smoke Test อยู่ที่ [[20260921-full-app-smoke-test-rerun-result]] (`.docs/03-testing/02-test-result/`)

---

## 1. สรุปภาพรวม

| # | ชุดทดสอบ | ทดสอบอะไร | จำนวน | ผล | เวลาที่รัน |
|---|---|---|---|---|---|
| A | `npm run lint` (ครั้งที่ 1) | ตรวจคุณภาพโค้ดด้วย ESLint | 1 | ❌ **ไม่ผ่าน** | 06:36:20 – 06:36:23 |
| B | `npm run build` | ตรวจ type ด้วย `tsc --noEmit` แล้ว build production ด้วย Vite | 1 | ✅ ผ่าน | 06:36:56 – 06:37:07 |
| C | `npm run test:rules` | ตรวจ Firestore Security Rules กับ Firestore Emulator | 1 | ⛔ **รันไม่ได้** (ติดสภาพแวดล้อม) | 06:37:32 – 06:37:42 |
| D | `npm run lint` (ครั้งที่ 2 — หลังเพิ่ม config) | เหมือนข้อ A | 1 | ✅ ผ่าน | 06:43:04 – 06:43:09 |
| E | Full App Smoke Test (เบราว์เซอร์จริง) | คลิกใช้งานจริงครบทั้ง 10 หน้าจอ + Quick Capture ตาม `spec.md` | 37 | ✅ ผ่านทั้งหมด | 06:36:55 – 06:52:53 |

**รวม: 40 รายการ — ผ่าน 39, ไม่ผ่าน 0, รันไม่ได้ 1**

> ข้อ A ที่ไม่ผ่าน ได้แก้ไขแล้วในรอบนี้และกลับมาผ่านที่ข้อ D — จึงไม่นับเป็น "ไม่ผ่าน" ที่ค้างอยู่
> เหลือค้างจริงเพียงข้อ C ซึ่งติดที่เครื่องไม่มี Java 21 ไม่ใช่ตัวกฎ Firestore มีปัญหา

---

## 2. ชุดที่รันด้วยคำสั่ง (A–D)

### A. `npm run lint` (ครั้งที่ 1) — ❌ ไม่ผ่าน · 06:36:20 – 06:36:23

**ทดสอบอะไร:** ให้ ESLint สแกนทั้งโปรเจกต์เพื่อหาโค้ดที่ผิดกฎ/มีความเสี่ยง

**ติดตรงไหน:** ESLint หา**ไฟล์ config ไม่เจอเลย** จึงไม่ได้เริ่มตรวจโค้ดแม้แต่ไฟล์เดียว (exit code 2)

```
ESLint couldn't find a configuration file.
ESLint looked for configuration files in D:\D\ADT-RISE\My-Today-Project-1\dist\assets and its ancestors.
```

**สาเหตุที่แท้จริง** (ตรวจยืนยันแล้ว ไม่ใช่การเดา):

| สิ่งที่ตรวจ | ผลการตรวจ |
|---|---|
| `eslint.config.js` (flat config) | ไม่มี |
| `.eslintrc` / `.eslintrc.cjs` / `.eslintrc.json` ฯลฯ | ไม่มี |
| คีย์ `eslintConfig` ใน `package.json` | ไม่มี |
| `git log --all` หาไฟล์ config ที่เคยถูก commit | **ไม่เคยมีเลยสักครั้ง** |
| devDependencies ที่เกี่ยวกับ ESLint | มีแค่ `eslint` ตัวเดียว ขาด parser/plugin สำหรับ TS+React ทั้งหมด |

แปลว่าคำสั่ง `npm run lint` ที่ระบุไว้ใน `CLAUDE.md` **ไม่เคยรันผ่านได้เลยตั้งแต่เริ่มโปรเจกต์** ไม่ใช่เพิ่งพัง

ส่วนที่ error ไปโผล่ที่ `dist/assets` เป็นอาการต่อเนื่องจากสาเหตุเดียวกัน — เมื่อไม่มี config ก็ไม่มีการตั้ง ignore ESLint จึงไล่สแกนเข้าไปใน build output ด้วย

### B. `npm run build` — ✅ ผ่าน · 06:36:56 – 06:37:07

**ทดสอบอะไร:** 2 ขั้นต่อกัน — `tsc --noEmit` ตรวจ type ทั้งโปรเจกต์ (ไม่ปล่อยไฟล์ออก) แล้วจึง `vite build` สร้าง production bundle จริง

**ผล:** ผ่านทั้งสองขั้น (exit 0) — ไม่มี type error, build สำเร็จใน 4.50 วินาที, 117 modules

| ไฟล์ที่ได้ | ขนาด | gzip |
|---|---|---|
| `dist/index.html` | 0.39 kB | 0.26 kB |
| `dist/assets/index-*.css` | 19.43 kB | 4.31 kB |
| `dist/assets/index-*.js` | 973.09 kB | 254.60 kB |

**หมายเหตุ (คำเตือน ไม่ใช่ข้อผิดพลาด):** Vite เตือนว่า JS chunk ใหญ่เกิน 500 kB และแนะนำให้ทำ code-splitting ด้วย dynamic `import()` หรือ `manualChunks` — ไม่ทำให้ build ล้มเหลวและไม่กระทบการทำงานของแอป แต่เป็นประเด็น performance ที่ควรพิจารณาในอนาคต (ยังไม่ได้แก้ในรอบนี้ เพราะอยู่นอกขอบเขตของการ "รันเทสต์")

### C. `npm run test:rules` — ⛔ รันไม่ได้ · 06:37:32 – 06:37:42

**ทดสอบอะไร:** รัน Firestore Emulator แล้วให้ `scripts/test-firestore-rules.mjs` ยิงคำสั่งอ่าน/เขียนเพื่อพิสูจน์ว่า `firestore.rules` บังคับ `request.auth.uid == uid` ได้จริง (ผู้ใช้ A ต้องอ่าน/เขียนข้อมูลของผู้ใช้ B ไม่ได้ และผู้ที่ยังไม่ sign in ต้องทำอะไรไม่ได้เลย)

**ติดตรงไหน:** ตัว emulator ไม่ยอมสตาร์ท เพราะ **เวอร์ชัน Java บนเครื่องเก่าเกินไป**

```
Error: firebase-tools no longer supports Java version before 21.
Please install a JDK at version 21 or above to get a compatible runtime.
```

| สิ่งที่ตรวจ | ค่าที่พบ |
|---|---|
| Java บนเครื่อง | `1.8.0_503` (Java 8) — `C:\Program Files\Java\jre1.8.0_503` |
| Java ที่ firebase-tools ต้องการ | 21 ขึ้นไป |
| Portable JDK 21 ที่เคยใช้เมื่อ 2026-09-05 | **ไม่เหลืออยู่แล้ว** (เคยวางไว้ใน temp directory ซึ่งถูกล้างไปแล้ว) |

**ข้อสรุปที่สำคัญ:** นี่**ไม่ใช่การที่กฎ Firestore ผิด** — ตัวกฎยังไม่ถูกทดสอบในรอบนี้เลยเพราะ runner ไม่สตาร์ท ครั้งล่าสุดที่ชุดนี้รันผ่านคือ 2026-09-05 (ผล "ALL CHECKS PASSED" บันทึกไว้ใน `backlog.md` Sprint 12) และ `firestore.rules` ก็ไม่ได้ถูกแก้ไขอีกเลยนับจากนั้น

**ต้องทำอะไรเพื่อให้รันได้:** ติดตั้ง JDK 21+ (เช่น Eclipse Temurin 21 แบบ `.zip` ที่ไม่ต้องสิทธิ์ admin) แล้วชี้ `JAVA_HOME`/`PATH` ไปที่ตัวนั้นก่อนรันคำสั่งเดิมซ้ำ — ยังไม่ได้ดำเนินการในรอบนี้เพราะต้องดาวน์โหลดไฟล์ติดตั้งจากอินเทอร์เน็ต ซึ่งควรให้เจ้าของเครื่องอนุมัติก่อน

### D. `npm run lint` (ครั้งที่ 2 หลังเพิ่ม config) — ✅ ผ่าน · 06:43:04 – 06:43:09

**สิ่งที่เพิ่มเข้าไปเพื่อแก้ข้อ A:**

1. ติดตั้ง devDependencies ที่ขาด 4 ตัว — `@typescript-eslint/parser@^7`, `@typescript-eslint/eslint-plugin@^7` (เวอร์ชัน 7 คือเวอร์ชันที่เข้ากับ ESLint 8), `eslint-plugin-react-hooks@^4`, `eslint-plugin-react-refresh@^0.4` (เดิมมีแต่ `eslint` เปล่าๆ ซึ่ง parse ไฟล์ `.ts`/`.tsx` ไม่ได้)
2. สร้างไฟล์ `.eslintrc.cjs` ที่ repo root — ใช้ฟอร์แมต eslintrc เพราะ ESLint 8 ยัง default เป็นแบบนี้ และต้องเป็นนามสกุล `.cjs` เพราะ `package.json` ตั้ง `"type": "module"` ไว้ (ถ้าใช้ `.eslintrc.js` จะถูกอ่านเป็น ESM แล้วพัง) ตั้ง ignore `dist`/`node_modules`/`.docs` และแยก override ให้ `api/**` กับ `scripts/**` ใช้ env ของ Node
3. **ไม่เปิด type-aware linting** (ไม่ใส่ `parserOptions.project`) เพราะ `npm run build` รัน `tsc --noEmit` ตรวจ type ให้อยู่แล้ว การตรวจซ้ำจะทำให้ lint ช้าโดยไม่จำเป็น

**ผล:** `exit 0` — **0 error, 0 warning**

**พิสูจน์ว่าไม่ได้ผ่านเพราะ config เป็นโมฆะ** (ตรวจ 2 ทาง):

| วิธีพิสูจน์ | ผล |
|---|---|
| นับไฟล์ที่ถูกตรวจจริง (`eslint . -f json`) | **68 ไฟล์** — `.tsx` 36, `.ts` 29, `.js` 2, `.mjs` 1 ครอบคลุมทั้ง `src/`, `api/smart-capture.ts`, `scripts/`, และไฟล์ config |
| ป้อนโค้ดที่ผิดกฎเข้าไปทาง stdin | จับได้จริง: `'unusedVar' is assigned a value but never used` (exit 1) โดยไม่มีการเขียนไฟล์ลงดิสก์ |

**ข้อสังเกต:** โค้ดเดิมของโปรเจกต์สะอาดอยู่แล้ว ไม่ต้องแก้ source แม้แต่ไฟล์เดียว — ที่ผ่านมาแค่ไม่เคยมี config ให้ ESLint ทำงานเท่านั้น

---

## 3. ชุด Full App Smoke Test ในเบราว์เซอร์ (E) — 37/37 ผ่าน

**รันเมื่อ:** 2026-09-21 เวลา 06:36:55 – 06:52:53 (รวม ~16 นาที) บน dev server `http://localhost:5173`
**วิธีทดสอบ:** คลิกใช้งานจริงในเบราว์เซอร์เหมือนผู้ใช้ทั่วไป (ไม่ใช่การอ่านโค้ดแล้วสรุปเอาเอง) ตรวจผลจากสิ่งที่เรนเดอร์จริง + console + ค่าที่เก็บใน LocalStorage/IndexedDB จริง
**เวลาในตารางด้านล่าง** อ้างอิงจาก `createdAt`/`updatedAt` จริงของ record ที่ถูกสร้าง/แก้ในแต่ละหมวด ไม่ใช่เวลาประมาณ

| TC | ทดสอบอะไร | ผล | เวลา |
|---|---|---|---|
| TC-01 | หน้า Dashboard โหลดครบทุก section (Summary Cards, Life Progress, แจ้งเตือน, งานวันนี้, ตารางวันนี้) | ✅ ผ่าน | 06:36:55 |
| TC-02 | สร้างงานใหม่สำเร็จ จำนวนงานเพิ่มขึ้นจริง | ✅ ผ่าน | 06:37:32 – 06:38:53 |
| TC-03 | กด "บันทึก" ฟอร์มงานโดยไม่กรอกอะไร → ต้องไม่สร้างงาน | ✅ ผ่าน | ″ |
| TC-04 | งานที่สร้างยังอยู่ครบหลัง reload หน้า (persistence) | ✅ ผ่าน | ″ |
| TC-05 | กดลบงานแล้ว "ยกเลิก" → งานต้องไม่หาย | ✅ ผ่าน | ″ |
| TC-06 | ตั้งค่า Life Area ให้งานผ่านฟอร์มแก้ไข แล้วบันทึกถูกต้อง | ✅ ผ่าน | ″ |
| TC-07 | หน้า Calendar (Today) แสดง event + task deadline รวมกันเรียงตามเวลา | ✅ ผ่าน | 06:39:53 |
| TC-08 | สร้างกิจกรรมใหม่สำเร็จ | ✅ ผ่าน | ″ |
| TC-09 | กด "บันทึก" ฟอร์มกิจกรรมโดยไม่กรอกอะไร → ต้องไม่สร้าง | ✅ ผ่าน | ″ |
| TC-10 | `?date=` ใน URL พาไปวันที่ระบุได้ถูกต้อง | ✅ ผ่าน | ″ |
| TC-11 | Timeline แบ่ง Now/Next/Later ถูกตามกฎ Smart Priority และไม่มีคำว่า "Score" | ✅ ผ่าน | 06:41 – 06:45 |
| TC-12 | กด Preview ไฟล์เดิมแล้วแสดงเนื้อหา inline ได้ | ✅ ผ่าน | 06:40:52 |
| TC-13 | กด "บันทึก" ฟอร์มอัปโหลดโดยไม่เลือกไฟล์ → ต้องไม่สร้าง | ✅ ผ่าน | ″ |
| TC-14 | อัปโหลดไฟล์จริงลง IndexedDB แล้วขึ้นในรายการ | ✅ ผ่าน | ″ |
| TC-15 | คลิกการแจ้งเตือน → พาไปยังงานนั้นและ mark read ให้ | ✅ ผ่าน | 06:41 – 06:45 |
| TC-16 | ปุ่ม "อ่านทั้งหมดแล้ว" mark read ครบทุกรายการ | ✅ ผ่าน | ″ |
| TC-17 | เมื่อไม่ได้ให้สิทธิ์ Browser Notification แอปยังใช้งานได้ปกติพร้อมข้อความแจ้ง | ✅ ผ่าน | ″ |
| TC-18/19 | จัดไฟล์ออกจาก Inbox เข้า Life Area แล้วย้ายไปหน้า `/files` ถูกต้อง | ✅ ผ่าน | ″ |
| TC-20 | จัดกิจกรรมออกจาก Inbox → ฟอร์มบังคับกรอก "วันที่" ที่ Quick Capture เคยข้ามไว้อีกครั้ง | ✅ ผ่าน | ″ |
| TC-21 | Inbox ว่างแล้วแสดง Empty State ถูกต้อง | ✅ ผ่าน | ″ |
| TC-22 | สร้าง Life Area ใหม่สำเร็จ | ✅ ผ่าน | 06:45:22 |
| TC-23 | กด "+ เพิ่ม" Life Area โดยไม่กรอกชื่อ → ต้องไม่สร้าง | ✅ ผ่าน | ″ |
| TC-24 | แก้ไขชื่อ Life Area สำเร็จ | ✅ ผ่าน | ″ |
| TC-25 | **กฎห้าม cascade delete** — ลบ Life Area แล้ว Task/Event/Note/Link ที่ผูกไว้ต้องอยู่ครบ เหลือแค่ `lifeAreaId` ว่าง | ✅ ผ่าน | ″ |
| TC-26 | เคลียร์ช่อง "ชื่อ" ให้ว่างแล้วกดบันทึกโปรไฟล์ → ฟอร์มต้องบล็อก | ✅ ผ่าน | 06:48:28 |
| TC-27 | บันทึกโปรไฟล์แล้วค่ายังอยู่หลัง reload | ✅ ผ่าน | ″ |
| TC-28 | สถานะยังไม่ sign in: แสดงปุ่ม Sign in และไม่มี toggle เปิด Cloud Sync | ✅ ผ่าน | ″ |
| TC-29 | ฟอร์ม Email/Password แสดงครบ กด submit เปล่าไม่ crash และไม่ยิงไป Firebase | ✅ ผ่าน | ″ |
| TC-30 | หน้า `/privacy` แสดงเนื้อหาคงที่ครบทุกหัวข้อ | ✅ ผ่าน | ~06:49 |
| TC-31 | ปุ่ม "+ Add to My Today" แสดงตัวเลือกครบทั้ง 5 ประเภท | ✅ ผ่าน | 06:49:53 – 06:51:32 |
| TC-32 | Smart Capture ประเภทงาน: ยังไม่ sign in → ขึ้นข้อความต้องเข้าสู่ระบบก่อน | ✅ ผ่าน | ″ |
| TC-33 | Smart Capture ประเภทกิจกรรม: เหมือน TC-32 | ✅ ผ่าน | ″ |
| TC-34 | ปุ่ม "กรอกฟอร์มเองแทน" เปิดฟอร์มปกติและบันทึกด้วยชื่ออย่างเดียวได้ | ✅ ผ่าน | ″ |
| TC-35 | ของที่สร้างผ่าน Quick Capture เข้า `/inbox` ก่อนเสมอ | ✅ ผ่าน | ″ |
| TC-36 | **กฎไม่รั่วไหล** — ของที่ยัง `inInbox: true` ต้องไม่โผล่ใน `/tasks`, `/calendar`, Dashboard | ✅ ผ่าน | ″ |
| TC-37 | Quick Capture ประเภทไฟล์: เข้า `/inbox` และไม่รั่วไปหน้า `/files` | ✅ ผ่าน | ″ |

**ไม่พบบั๊กของแอปพลิเคชันเลย** — ไม่มี console error แม้แต่จุดเดียวทั้ง 10 หน้า ผลตรงกับรอบทดสอบก่อนหน้า (2026-09-20/21) ทุกประการ

---

## 4. ข้อที่ไม่ผ่าน — สรุปว่าติดตรงไหน

| ข้อ | สถานะ | ติดตรงไหน | ต้องทำอะไรต่อ |
|---|---|---|---|
| A — `npm run lint` | ❌ ไม่ผ่าน → ✅ **แก้แล้วในรอบนี้** | ไม่มีไฟล์ ESLint config ในรีโปเลย (และไม่เคยมี) + ขาด parser/plugin สำหรับ TS+React | แก้แล้ว: เพิ่ม `.eslintrc.cjs` + ติดตั้ง devDependencies 4 ตัว → กลับมาผ่านที่ข้อ D |
| C — `npm run test:rules` | ⛔ **ยังค้างอยู่** | Java บนเครื่องเป็นเวอร์ชัน 8 แต่ firebase-tools ต้องการ 21+ → Firestore Emulator ไม่สตาร์ท ทำให้ตัวกฎยังไม่ได้ถูกทดสอบในรอบนี้ | ติดตั้ง JDK 21+ แล้วตั้ง `JAVA_HOME`/`PATH` ก่อนรันคำสั่งเดิมซ้ำ (ต้องดาวน์โหลดตัวติดตั้ง — รอเจ้าของเครื่องอนุมัติ) |

ไม่มีข้อใดที่ "ไม่ผ่าน" เพราะตัวแอปทำงานผิด

---

## 5. สิ่งที่ยังไม่ได้ทดสอบ (โดยเจตนา)

ต้องใช้บัญชีจริงในการ sign in จึงอยู่นอกขอบเขตของรอบนี้ ไม่ได้กรอกอีเมล/รหัสผ่านจริงใดๆ ทั้งสิ้น:

- การ sign in สำเร็จจริง (ทั้ง Google และ Email/Password)
- การเปิด Cloud Sync จริง และการ sync ข้ามอุปกรณ์จริง
- การเรียก Smart Capture ไปยัง Gemini API จริง (ทดสอบได้แค่ "สถานะยังไม่ sign in" คือ TC-32/TC-33)

---

## 6. หมายเหตุเพื่อความโปร่งใส

**สิ่งที่ถูกจำลองในการทดสอบเบราว์เซอร์** — จำลองเฉพาะส่วนที่เป็นของเบราว์เซอร์/ระบบปฏิบัติการซึ่งเครื่องมือ automation เข้าไม่ถึง ไม่ได้แตะตรรกะของแอป:

1. **Native confirm dialog** — stub `window.confirm` ให้คืนค่า `true` ก่อนกดปุ่มลบ (ใช้กับ TC-25) ส่วนการคลิกปุ่มและตรรกะการลบเป็นของแอปจริง
2. **OS file picker** — สร้าง `File` ใน memory แล้วใส่เข้า `input[type=file]` ผ่าน `DataTransfer` แล้ว dispatch `change` (ใช้กับ TC-14, TC-37) พิสูจน์ว่า handler ของแอปทำงานจริงจากการที่แอปเติมชื่อไฟล์ให้อัตโนมัติ
3. **การกดลบตัวอักษรในช่องกรอก** (พบใหม่รอบนี้) — `Backspace`/`Ctrl+A` ของเครื่องมือใช้ไม่ได้กับ React controlled input บางตัว จึงใช้ native value setter + dispatch `input` event แทนเพื่อเคลียร์ช่อง "ชื่อ" ก่อนทดสอบ required-field (ใช้กับ TC-26) ตรรกะ validate เป็นของแอปจริงทั้งหมด

**ไม่มีการแก้ไขซอร์สโค้ดของแอปเพื่อให้เทสต์ผ่าน** — ไฟล์ที่ถูกแก้/เพิ่มในรอบนี้มีเพียง `.eslintrc.cjs`, `package.json`, `package-lock.json` (จากการติดตั้ง ESLint plugins) และไฟล์รายงานผลทดสอบเท่านั้น

**ข้อมูลทดสอบตกค้าง:** dev server มีข้อมูลทดสอบจากรอบก่อนหน้าค้างอยู่ตั้งแต่ก่อนเริ่ม (ทำให้ตัวเลขจำนวนรายการในรายงานไม่ตรงกับการติดตั้งใหม่สะอาดๆ) และรอบนี้เพิ่มของที่ลงท้ายด้วย `-R2` เข้าไปอีก ทั้งหมดอยู่ใน LocalStorage/IndexedDB ของเครื่องเท่านั้น ไม่กระทบ production — ควรล้าง site data ก่อนนำไปสาธิตจริง

---

## Change Log

- 2026-09-21 — สร้างเอกสารนี้: รวมผลการรันเทสต์ทุกชุดของโปรเจกต์ในรอบเดียว (lint, build, test:rules, Full App Smoke Test 37 เคส) พบ `npm run lint` ใช้งานไม่ได้มาตลอดและแก้ไขแล้วในรอบเดียวกัน, `npm run test:rules` ยังค้างเพราะเครื่องไม่มี Java 21, ส่วน build และ Smoke Test ผ่านทั้งหมด ไม่พบบั๊กของแอปพลิเคชัน
