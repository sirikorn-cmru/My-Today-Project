# ACL.md — สิทธิ์การเข้าถึงของระบบ My Today

> **หมายเหตุสำคัญ:** My Today เป็นแอป **Personal Daily Workspace แบบผู้ใช้เดียว (single-user)** ไม่มีระบบ Role/Admin-User ในความหมายทั่วไป (ไม่มีผู้ดูแลระบบที่เห็น/จัดการข้อมูลของผู้ใช้คนอื่นได้) ตารางด้านล่างจึงไม่ใช่ "2 บทบาท Admin/User" แต่คือ **2 สถานะการใช้งานจริงของผู้ใช้คนเดียวกัน** ตามที่ระบบพิสูจน์ตัวตนได้จริง (sign in แล้วหรือยัง) — สิทธิ์เดียวที่ระบบบังคับจริงคือ **ผู้ใช้แต่ละคนเข้าถึงได้เฉพาะข้อมูลของตัวเองเท่านั้น ไม่มีใครเห็นข้อมูลของอีกคนได้** (บังคับที่ระดับ Firestore Security Rules ไม่ใช่แค่ฝั่ง UI)

## ตารางสิทธิ์

| สถานะผู้ใช้ | ทำได้ | ทำไม่ได้ |
|---|---|---|
| **ผู้ใช้ที่ยังไม่ sign in** (ค่าเริ่มต้นของแอป — ไม่บังคับต้องมีบัญชี) | สร้าง/แก้ไข/ลบ Task, Event, File, Note, Link, Life Area, Personal Profile ได้ครบทุกอย่าง (ข้อมูลเก็บในเครื่องผ่าน LocalStorage/IndexedDB เท่านั้น) | เปิดใช้ Cloud Sync (ต้อง sign in ก่อนเสมอ); ใช้ฟีเจอร์ Smart Capture จากรูปภาพ (ต้อง sign in ก่อนเสมอ); ข้อมูลจะไม่ถูกสำรอง/ซิงก์ข้ามอุปกรณ์ |
| **ผู้ใช้ที่ sign in แล้ว** (Google หรือ Email/Password — สิทธิ์เท่ากันทั้งสองวิธี ไม่มีวิธีไหนได้สิทธิ์เพิ่ม) | ทำได้ทุกอย่างของแถวบน **บวก**: เปิด Cloud Sync แล้ว sync ข้อมูล (Task/Event/Note/Link/Life Area/Profile) ข้ามอุปกรณ์ได้; ใช้ Smart Capture จากรูปภาพได้; อ่าน/เขียน/ลบข้อมูลของ**เฉพาะบัญชีตัวเอง**บน Firestore ได้เต็มรูปแบบ | อ่านหรือแก้ไขข้อมูลของบัญชีอื่นบน Firestore ไม่ได้เด็ดขาด (`firestore.rules` บังคับ `request.auth.uid == uid` ทุก path — ปฏิเสธ request ทันทีถ้า uid ไม่ตรงเจ้าของ ไม่ว่าจะพยายามผ่านช่องทางไหนก็ตาม); ไม่มีสิทธิ์ระดับ "ผู้ดูแลระบบ" ใดๆ ที่มองเห็นภาพรวมผู้ใช้ทุกคนในระบบ — ไม่มีบทบาทแบบนั้นอยู่จริง |

## อ้างอิงกฎที่บังคับจริง

- `firestore.rules` (repo root): `allow read, write: if request.auth != null && request.auth.uid == uid;` ภายใต้ path `users/{uid}/{document=**}` — นี่คือกฎสิทธิ์ **เดียว** ที่มีอยู่จริงในระบบ ไม่มีกฎอื่นซ้อนทับ (ไม่มี custom claims, ไม่มี role field ในเอกสารผู้ใช้)
- ยืนยันด้วย `npm run test:rules` ผ่าน Firestore Emulator แล้ว (ดู `.docs/01-requirements/backlog.md`'s Sprint 12 entry): ผู้ใช้ A เขียน/อ่านข้อมูลของตัวเองได้ แต่ผู้ใช้ B อ่าน/เขียนข้อมูลของ A ไม่ได้เลย และผู้ใช้ที่ไม่ signed in อ่าน/เขียนอะไรบน Firestore ไม่ได้เลย
- `src/hooks/useAuth.ts` — ทั้ง Google Sign-In (Sprint 12) และ Email/Password (Sprint 14) คืน Firebase `User` object รูปแบบเดียวกัน จึงได้สิทธิ์เท่ากันทุกประการ ไม่มีวิธี sign in ไหนได้สิทธิ์พิเศษกว่ากัน

## ถ้าต้องการระบบ Admin/User จริง

ถ้าในอนาคตต้องการเพิ่มบทบาทผู้ดูแลระบบ (เช่น ดูข้อมูลผู้ใช้ทุกคน, ปิดการใช้งานบัญชี ฯลฯ) นี่คือการเปลี่ยนขอบเขตของผลิตภัณฑ์จริง (My Today วางตำแหน่งเป็น Personal Daily Workspace ของคนคนเดียว ไม่ใช่ระบบที่มีผู้ดูแลกลาง) ต้องเปิดผ่านกระบวนการ `requirement-intake` เป็น Sprint ใหม่ ไม่ใช่แค่แก้เอกสารนี้
